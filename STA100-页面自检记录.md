# STA-100 页面与 OpenClaw 自检记录

检查日期：2026-08-11  
检查目录：`/home/User/gsx/gitdir/std`

## 1. OpenClaw 部署

| 检查项 | 结果 | 核验内容 |
|---|---|---|
| 官方最新稳定版 | 通过 | npm `latest` 与 GitHub 最新 Release 均为 `2026.7.1-2`；Release 发布时间 2026-08-04 |
| 官方包完整性 | 通过 | npm tarball SHA-256、npm integrity、Node.js ARM64 SHA-256 与元数据一致 |
| GitHub 完整源码 | 通过 | `v2026.7.1-2` codeload 归档 tar 校验通过，SHA-256 已记录；`upstream/` 共 21,891 个源码/测试/构建文件 |
| 独立运行时 | 通过 | Node.js `22.22.3` ARM64，OpenClaw `2026.7.1-2 (0790d9f)` |
| 旧安装清理 | 通过 | `/usr/local/bin/openclaw` 和 `/usr/local/lib/node_modules/openclaw` 均不存在 |
| 客户状态保护 | 通过 | `~/.openclaw` 未删除；每次部署前在 `openclaw/backups` 创建 tar.gz 和 SHA-256 |
| systemd | 通过 | `openclaw-gateway.service` enabled/active/running，实际使用本目录 Node.js；配置审计 `ok=true` |
| Gateway | 通过 | 仅监听 `127.0.0.1/[::1]:18789`，RPC `ok=true`，版本无漂移 |
| 健康检查 | 通过 | `openclaw health --json` 返回 `ok=true`，插件无加载错误 |
| Agent | 通过 | 实际列表为 `main + 24`；24 个业务 Agent 均有独立工作区、中文身份、原应用 Emoji 和默认模型 |
| 重复同步 | 通过 | 第二次执行全部显示 `unchanged`，约 32 秒完成，不重复创建 Agent |

## 2. Go 服务与接口

| 检查项 | 结果 | 核验内容 |
|---|---|---|
| JavaScript 语法 | 通过 | `node --check assets/app.js` 无错误 |
| Go 格式与测试 | 通过 | `gofmt`、`go test ./...`、`go vet ./...` 均通过 |
| 单元测试 | 通过 | 覆盖模型引用解析、OpenClaw 敏感配置隔离、写操作请求头拦截、Agent 消息文件与回复解析 |
| ARM64 构建 | 通过 | `CGO_ENABLED=0 GOOS=linux GOARCH=arm64` 静态构建成功 |
| 健康接口 | 通过 | `/api/health` 返回 `status=ok`、`service=sta100-web`、`prototype=false` |
| 状态接口 | 通过 | 返回 OpenClaw `2026.7.1-2`、systemd running、RPC 正常、审计通过 |
| 模型接口 | 通过 | 返回默认模型 `deepseek/deepseek-v4-flash`、4 个可用模型、DeepSeek 凭据已配置 |
| Agent 接口 | 通过 | 返回 25 条，其中 24 个业务 Agent 和 24 个 Emoji；安全字段读取约 4 ms |
| Agent 同步接口 | 通过 | 无 `X-STA100-Request` 返回 403；携带请求头后同步成功并返回 25 条 |
| 凭据泄露检查 | 通过 | 状态、模型和 Agent 响应中未发现 API Key、Gateway token 或密钥值 |
| 本机账户接口 | 通过 | 默认 `admin/admin`；未登录访问 OpenClaw API 返回 401，登录后 200；错误万能密码 401，正确验证不修改账户 |
| 账户存储 | 通过 | `~/.config/sta100/auth.json` 权限 0600；密码为随机盐和 150,000 轮迭代哈希，无明文密码 |
| API Key 链路 | 通过 | 依据官方文档使用 stdin；Key 不进入 CLI 参数、响应或日志；未覆盖客户现有 Key 做破坏性测试 |

## 3. 页面与交互

| 检查项 | 结果 | 核验内容 |
|---|---|---|
| 页面入口 | 通过 | 11 个页面可由 URL hash 进入；新增供应商页面；浏览器前进/后退可同步页面 |
| 登录与恢复 | 通过 | 未登录时工作台隐藏；登录页显示当前用户名；支持密码登录、退出、设置页修改账户及万能密码重置 |
| Emoji 与图标 | 通过 | 左上使用旧应用 1024x1024 骑行图标；11 个主模块和 24 个 Agent 使用原应用 Emoji；按钮使用本地 Lucide |
| 字号与颜色 | 通过 | 正文字号提高；页面采用白色内容面、浅灰工作区、深灰正文、粉色品牌主色、蓝色交互色和绿色成功状态，模块用低饱和语义色区分 |
| OEM 来源 | 通过 | 页面统一为本地知识库/联网检索；RAG 是联网检索方式，不单独并列 |
| 概览工具标题 | 通过 | OEM、客户统一搜索、本地客户发现只保留 Emoji，没有旧快捷图片节点 |
| 新闻设置 | 通过 | 数量 1-100 整数校验；频率限定 1/2/3/6/8/12/24 小时；超过 20 条列表内部滚动 |
| 客户发现范围 | 通过 | 国家字典覆盖中国及欧洲国家，城市随国家联动且保持单选 |
| Agent 聊天来源 | 通过 | 三类来源为复选选项；联网白名单可按 Agent 查看、编辑和保存到本机浏览器 |
| 关系模糊搜索 | 通过 | 报价关联客户输入 `velo` 只返回 `VeloTrade GmbH`；订单和单据使用同一关系搜索组件 |
| 表头排序与选择 | 通过 | 客户订单数量/累计金额/最近更新、报价金额、订单金额均在字段旁用箭头切换；全选、半选、行选择与筛选结果联动，排序后选择不丢失 |
| 嵌套弹层 | 通过 | 从客户关联订单详情点击生成单据后，订单抽屉关闭且单据弹窗完整可见；弹窗内辅助抽屉仍可覆盖并返回 |
| 产品业务链 | 通过 | 报价/订单可选择 3 个启用产品并新增多行；选择骑行码表 2 件自动得到 EUR 438.00，保存后订单摘要和明细一致；单据读取订单快照 |
| 客户关联详情 | 通过 | 客户详情报价、订单、单据标签可切换，关联行有详情动作并可进入对应抽屉 |
| 业务 CRUD | 通过 | 客户、报价、订单、单据、产品、供应商新增保存均更新前端状态；编辑、详情、删除动作已接入 |
| 供应商页面 | 通过 | 支持规定字段、关键字搜索、公司/更新时间排序、详情、编辑、删除和 CSV 导出 |
| 单据搜索 | 通过 | 可搜索单据编号、客户、订单、模板；初始 4 条数据完整显示 |
| 单据组合筛选 | 通过 | CI 筛选只返回 `CI-20260809-012`；VeloTrade + Review 返回 `CD-20260808-006` |
| 单据空状态 | 通过 | 无匹配时显示空状态，可从顶部或空状态重置全部条件 |
| 模板入口 | 通过 | 图片和文件模板入口独立；格式/20 MB 校验、待接口状态、4 个未实现接口及原因均可见，不显示虚假上传成功 |
| 模型设置 | 通过 | 页面显示真实默认模型和 4 个模型选项；已保存 Key 默认隐藏且只可查看配置状态，新 Key 输入默认隐藏并可切换显示 |
| Agent 管理 | 通过 | 管理弹窗显示 24 行真实 Agent，可执行清单同步 |
| 桌面布局 | 通过 | Chromium 1440x1000 遍历 11 个页面，检查概览、模型设置及业务页面，无页面级横向溢出或重叠 |
| 设置排版 | 通过 | 二级导航 228px、14px 字号、48px 高度并带选中边界；移动详情值和按钮换行后无逐字挤压 |
| 移动布局 | 通过 | Chromium 390x844 检查设置页，document/body 宽度均为 390px；侧栏收起后无残留，宽表仅在表格容器内滚动 |
| 浏览器错误 | 通过 | 页面测试期间无 console error 和 page error |
| 浅色主题覆盖 | 通过 | Chromium 实测登录、概览、客户、智能体、设置、新建客户弹窗、Agent 聊天和移动导航；主题变量为 `#f7f7f9 / #ffffff / #202124 / #ed3f93 / #2378ed / #2ca56d`，无页面级横向溢出 |
| 真实 Agent 调用 | 外部配置阻断 | `/api/v1/agents/chat` 已真实进入 OpenClaw Gateway；当前 `deepseek/deepseek-v4-flash` 返回账户额度不足，页面显示失败并提供重试，不伪造答案 |

## 4. 数据边界

OpenClaw 服务状态、模型和 Agent 数据为真实本机数据。客户、报价、订单、单据、产品、供应商、文件、新闻、OEM 和客户发现结果仍为前端原型数据；页面 CRUD 保存在 JavaScript 内存中，刷新页面或重启服务后恢复示例数据。页面交互可验收，但不能把这些演示记录作为真实业务数据。

## 5. 未做的破坏性验证

- 未用测试 Key 覆盖客户现有 DeepSeek 凭据；只验证官方 stdin 实现、后端校验和页面空 Key 行为。
- 未切换默认模型，避免改变 24 个 Agent 当前继承的 `deepseek/deepseek-v4-flash`。
- 未执行真实离线升级安装；升级包签名、清单、回滚和重启仍待正式升级包后联调。
- 未调用模板上传、OCR、字段映射和发布接口；这些后端接口尚未实现，且正式模板样例、映射规范和识别要求尚未提供。

其余业务接口、SQLite、真实单据模板和正式数据接入 TODO 见 `STA100-详细设计说明书.md` 第 10 节。
