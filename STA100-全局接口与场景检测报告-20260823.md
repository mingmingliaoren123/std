# STA-100 全局接口与场景检测报告

检测时间：2026-08-23  
检测范围：`sta100-web`、`openclaw-orchestrator`、`scripts`、`release`、当前 `openclaw` 工作目录  
检测原则：本次只检测和记录问题，不修改业务逻辑代码。

## 一、总体结论

当前应用的基础编译、Go 单元测试、前端 JS 语法和打包脚本语法均通过；`127.0.0.1:18080` 当前服务可访问，健康检查正常。

主要风险集中在四类：

1. 有一批接口明确等待客户资料或业务规范，当前返回 `501 TODO_*`，属于“保留入口但不能假成功”的合理状态。
2. Agent 场景已经接入 OpenClaw，但“后台最多 2 个 Agent 并行，多余排队”的后端统一队列未看到完整实现。
3. 模型、模板、本地客户发现等说明文档与当前代码实现存在不一致，容易影响验收认知。
4. 部署包已包含 Go 程序、OpenClaw app、node_modules、Agent 初始化配置和启动脚本，但不包含 Node 运行时；新机器必须预装满足 OpenClaw 要求的 Node。

## 二、检测命令与结果

| 检测项 | 命令/方式 | 结果 |
|---|---|---|
| STA-100 Go 测试 | `cd sta100-web && go test ./...` | 通过 |
| OpenClaw 编排层测试 | `cd openclaw-orchestrator && go test ./...` | 通过 |
| 前端 JS 语法 | `node --check sta100-web/assets/app.js` | 通过 |
| 打包脚本语法 | `bash -n scripts/package-release.sh` | 通过 |
| release 启停脚本语法 | `bash -n release/sta100-release/scripts/sta100.sh` | 通过 |
| 当前服务健康检查 | `curl http://127.0.0.1:18080/api/health` | 返回 `status=ok` |
| 当前登录状态 | `curl http://127.0.0.1:18080/api/v1/auth/status` | 返回 `authenticated=true, username=admin` |
| 浏览器 smoke | `node sta100-web/scripts/browser-smoke.mjs` | 本次未执行页面遍历，原因是本机未启动 Chrome DevTools `127.0.0.1:9223`，属于测试前置条件缺失 |

## 三、问题清单

| 编号 | 模块/场景 | 问题描述 | 证据位置 | 影响 | 建议处理 | 优先级 | 是否需确认 |
|---|---|---|---|---|---|---|---|
| G-001 | Agent 并发执行 | 用户曾要求后台最多 2 个 Agent 同时运行，多余消息进入队列。当前前端只通过 `state.agentChatPending[agentID]` 限制“同一个 Agent 连续发送”，未看到后端统一的全局并发队列或信号量。 | `sta100-web/assets/app.js:3245`、`sta100-web/assets/app.js:3264`；后端 `sta100-web/api_assistant.go` 未发现全局队列实现 | 多个不同 Agent 可能同时打到 OpenClaw，导致慢、超时、额度压力或响应错峰 | 增加后端全局 Agent 调用队列：最大并发 2，队列状态可返回给前端；前端展示“排队中/执行中/完成/失败” | 高 | 否，属于已提出需求 |
| G-002 | 待客户资料接口 | 名片 OCR、产品批量导入、报价发送、私有文件摘要、OEM 数据、旧客户发现、RAG 客户搜索、离线升级包等仍返回 `501 TODO_*`。 | `sta100-web/api_master_data.go:21`、`sta100-web/api_master_data.go:466`、`sta100-web/api_commerce.go:171`、`sta100-web/api_extended.go:76`、`sta100-web/api_extended.go:504`、`sta100-web/api_extended.go:511`、`sta100-web/api_extended.go:900`、`sta100-web/api_extended.go:2096` | 页面入口若用户点击，会看到“待资料/未实现”提示；不能作为完整交付能力验收 | 维持不伪造成功；等客户给 OCR、导入模板、工厂库、升级包规范等资料后逐项实现 | 高 | 是 |
| G-003 | 本地客户发现/OpenClaw 能力边界 | 当前 Go 侧已按固定输入输出把国家、城市、类型、返回条数交给 `customer-measurement-agent`，且客户发现不读取本机客户库。但能否取到国外客户取决于 OpenClaw 客户发现 Agent 自身的公开来源/联网能力。 | 前端提交：`sta100-web/assets/app.js:721`；后端固定提示：`sta100-web/api_assistant.go:502`；客户发现不读本机库：`sta100-web/api_assistant.go:993`；调用来源覆盖为联网检索：`sta100-web/api_assistant.go:532` | 如果 OpenClaw Agent 没有可用公开来源能力，会返回空 items 或“未完成”，页面不能凭空生成客户 | 需要给客户确认公开来源策略和 OpenClaw 侧工具能力；应用侧继续严格解析，不编造客户 | 中高 | 是 |
| G-004 | 客户发现来源函数存在易误读设计 | `assistantAgentSources(agentID string)` 内部判断 `normalizedFeature(agentID) == "customer-discovery"`，从函数签名看传入的是 Agent ID，不是 feature。当前客户发现调用处又单独覆盖了 `sources = []string{"联网检索"}`，所以运行上暂不构成缺陷，但代码可读性容易误导后续维护。 | `sta100-web/api_assistant.go:916`、`sta100-web/api_assistant.go:532` | 后续改动时可能误以为该函数能自动识别客户发现 feature | 后续可把函数改为接收 `request` 或明确命名为 `assistantSourcesForFeature` | 中 | 否 |
| G-005 | Agent 完成进度条立即清除 | `finishAgentProgress` 设置完成状态后立即 `delete state.agentChatProgress[agentID]`。消息下方的阶段耗时来自已持久化消息 `pipeline`，不是独立运行进度状态。 | `sta100-web/assets/app.js:3213`、`sta100-web/assets/app.js:3229`、消息渲染 `sta100-web/assets/app.js:2944` | 这符合“回复完成后运行进度不要再占一行”的方向，但如果后续还要保留完成态进度条，需要重新确认 | 当前建议保持；若用户要求完成后仍显示独立进度条，再改前端状态策略 | 中 | 是 |
| G-006 | 模型目录文档与代码数量不一致 | 代码固定目录当前统计为 91 个模型、18 个 provider、版本 `openclaw-2026.7.1-2`；README 和接口说明仍写“20 个 API Key 提供商、106 个文本模型”。 | 代码：`openclaw-orchestrator/orchestrator/model_catalog.go:5`；文档：`sta100-web/README.md:68`、`STA100-后端接口实现与验证说明.md:156` | 交付时客户会认为模型少了或文档不可信 | 更新文档口径，或重新从当前 OpenClaw 版本导出完整目录后同步代码和文档 | 中 | 否 |
| G-007 | 模板中心文档过期 | README 仍写模板上传、OCR、字段映射、发布返回 `501`；但当前 `api_templates.go` 已实现模板上传、列表、编辑、删除、发布默认、预览渲染。 | 过期文档：`sta100-web/README.md:76`；当前实现：`sta100-web/api_templates.go:25`、`sta100-web/api_templates.go:78`、`sta100-web/api_templates.go:159`、`sta100-web/api_templates.go:217`、`sta100-web/api_templates.go:256` | 验收时会误认为模板功能未做 | 更新 README 和接口说明；同时明确 OCR 只是“图片模板上传记录”，不是完整 OCR 识别 | 中 | 否 |
| G-008 | 本地客户发现文档过期 | README 仍写“国家、城市和客户类型单选”；当前前端已经是国家单选、城市多选、客户类型多选。 | 文档：`sta100-web/README.md:73`；前端：`sta100-web/assets/app.js:721`、`sta100-web/assets/app.js:735`、`sta100-web/assets/app.js:736` | 客户确认字段时容易误解页面实际能力 | 更新文档为：国家单选，城市/类型多选，返回数量 1-100 | 中 | 否 |
| G-009 | 部署包 Node 前置条件未写清楚 | release 包包含 `openclaw/app/node_modules` 和 `openclaw/bin/openclaw`，但不包含 Node 运行时。部署 README 未明确目标机 Node 版本要求。 | 包内容：`release/sta100-release-linux-arm64-20260823-123112.tar.gz`；文档：`release/README-DEPLOY.md:20`、`scripts/package-release.sh:237` | 新机器无 Node 时，OpenClaw CLI 可能无法运行，Agent/模型/通道功能不可用 | 在部署 README 和打包脚本内置说明中补充 Node 前置条件；启动脚本启动前检测 Node 版本并给明确错误 | 中 | 是 |
| G-010 | release 包内包含运行后数据文件 | 当前 `release/sta100-release` 目录里已有 `data/sta100.db`、`data/auth.json`、`logs/sta100-web.log` 等运行产物。压缩包需要确认是否来自“全新应用”打包状态。 | `release/sta100-release/data/auth.json`、`release/sta100-release/data/sta100.db`、`release/sta100-release/logs/sta100-web.log` | 如果这些文件进入最终部署包，可能不符合“新部署应用没有人工操作和配置”的要求 | 打包脚本生成压缩包前应清理 data/logs 或只保留空目录；本轮不删除，等待确认 | 中 | 是 |
| G-011 | OpenClaw 工作区历史输出较多 | `openclaw/workspaces/sta100-news-curator/output`、`sta100-recommend-curator/output` 存在多份历史新闻/推荐输出文件。 | `git status --short` 显示多份 `openclaw/workspaces/.../output/*.json` | 若误打进 release 或提交仓库，会干扰“清理历史数据/全新应用”的目标 | 确认这些是否应加入忽略或清理；正式打包时不要带 workspace 历史输出 | 中 | 是 |
| G-012 | OpenClaw backups 目录存在历史备份包 | `openclaw/backups` 下存在多个 tar.gz 备份。 | `openclaw/backups/openclaw-state-*.tar.gz` | 可能增加仓库和备份体积；若误打包会泄露旧环境状态 | 确认保留策略，建议不进入 release，不提交远端 | 低 | 是 |
| G-013 | 浏览器视觉检查脚本健壮性不足 | 继承检测记录中 `browser-visual-check.mjs` 曾因读取不存在的 `appShell.hidden` 空指针失败；本次 `browser-smoke` 也因未启动 9223 无法执行。 | `sta100-web/scripts/browser-visual-check.mjs`；本次错误为 `ECONNREFUSED 127.0.0.1:9223` | 自动化视觉回归不能稳定执行，不代表业务页面本身失败 | 修复脚本等待逻辑；补充“启动 headless Chrome DevTools”的一键脚本 | 低 | 否 |
| G-014 | 报价发送仍待正式业务规范 | 报价发送接口只校验报价存在且为 Draft，然后返回 `TODO_QUOTE_DELIVERY`，不会修改状态冒充发送成功。 | `sta100-web/api_commerce.go:158`、`sta100-web/api_commerce.go:167`、`sta100-web/api_commerce.go:171` | 报价无法真实邮件发送，报价状态流转到 Sent 仍需规则 | 客户提供报价模板、邮件主题正文、发件账号、收件人规则和重试规则后实现 | 中 | 是 |
| G-015 | 离线升级仍待包规范 | 离线升级导入/安装入口返回 `TODO_UPGRADE_PACKAGE_SPEC`，未实现真实安装、迁移、回滚、自动重启。 | `sta100-web/api_extended.go:2093`、`sta100-web/api_extended.go:2096` | 设置页离线升级不能作为正式可用能力验收 | 需要定义 manifest、签名算法、兼容版本、迁移脚本、失败回滚验收 | 高 | 是 |

## 四、待客户资料或规范的接口清单

| 接口/场景 | 当前状态 | 缺少内容 | 代码位置 |
|---|---|---|---|
| `POST /api/v1/accounts/ocr` | 返回 `501 TODO_OCR_SPEC` | OCR 引擎、名片样例、字段映射、置信度阈值 | `sta100-web/api_master_data.go:21` |
| `POST /api/v1/products/import` | 返回 `501 TODO_PRODUCT_IMPORT_FORMAT` | 产品导入模板、必填字段、重复编码处理、错误回执格式 | `sta100-web/api_master_data.go:466` |
| `POST /api/v1/quotes/{id}/deliver` | 返回 `501 TODO_QUOTE_DELIVERY` | 报价模板、PDF 版式、邮件主题正文、发件账户、收件规则、失败重试 | `sta100-web/api_commerce.go:171` |
| `GET /api/v1/private-files/{id}/summary` | 返回 `501 TODO_PRIVATE_FILE_PARSER` | 原始文件格式、解析器、摘要字段、索引策略 | `sta100-web/api_extended.go:76` |
| `POST /api/v1/overview/oem/match` | 返回 `501 TODO_OEM_DATA` | 工厂原始数据、骑行类目、评分权重、报告格式 | `sta100-web/api_extended.go:504` |
| `GET /api/v1/overview/oem-matches/{id}` | 返回 `501 TODO_OEM_DATA` | 工厂详情字段、来源链接、认证字段、抓取时间 | `sta100-web/api_extended.go:517` |
| `POST /api/v1/overview/customers/search` 的 `rag/mixed` 模式 | 返回 `501 TODO_RAG_DATA` | RAG 数据格式、联网来源、去重规则、字段来源展示 | `sta100-web/api_extended.go:900` |
| `POST /api/v1/system/upgrade/import/install` | 返回 `501 TODO_UPGRADE_PACKAGE_SPEC` | manifest、签名、兼容版本、迁移脚本、回滚验收 | `sta100-web/api_extended.go:2096` |

## 五、已核实非问题或当前合理设计

| 场景 | 核实结论 | 证据 |
|---|---|---|
| 客户发现是否还读取本机客户库 | 当前客户发现不读取本机客户库，`collectLocalEvidence` 对 `customer-discovery` 直接返回 nil。 | `sta100-web/api_assistant.go:992` |
| 客户发现是否使用固定输入输出 | 已实现固定输入：国家、城市、客户类型、返回条数；固定输出字段包括 `name,country,city,type,business,contact,phone,email,website,address,source,sourceUrl,updatedAt,score,reason`。 | `sta100-web/api_assistant.go:502` 到 `sta100-web/api_assistant.go:528` |
| 客户发现调用来源 | 当前调用处明确将来源覆盖为 `联网检索`。 | `sta100-web/api_assistant.go:532` |
| Agent 聊天记录 | 前端会从 `/api/v1/assistant/history` 读取历史消息，并保留消息 `pipeline` 和 `totalDurationMs`。 | `sta100-web/assets/app.js:3134` 到 `sta100-web/assets/app.js:3148` |
| Emoji 资源 | 前端资源通过 Go embed 打进二进制，`NotoColorEmoji.ttf` 位于 `sta100-web/assets/NotoColorEmoji.ttf`，压缩包中看不到独立 assets 文件并不等于丢失。 | `sta100-web/main.go:14`、`sta100-web/main.go:17` |
| 模板管理 | 当前模板管理已不是纯 TODO，已具备上传、列表、编辑、发布默认、预览渲染等后端接口。 | `sta100-web/api_templates.go:25` 到 `sta100-web/api_templates.go:256` |

## 六、部署包检查

当前包：`release/sta100-release-linux-arm64-20260823-123112.tar.gz`

| 检查项 | 结果 |
|---|---|
| 压缩包大小 | 约 88 MB |
| 解包目录大小 | 约 521 MB |
| 是否包含 STA-100 Go 程序 | 包含 `sta100-release/bin/sta100-web` |
| 是否包含 OpenClaw 编排工具 | 包含 `sta100-release/bin/openclaw-operator` |
| 是否包含 OpenClaw CLI 入口 | 包含 `sta100-release/openclaw/bin/openclaw` |
| 是否包含 OpenClaw app/node_modules | 包含 |
| 是否包含 Node 运行时 | 未包含 |
| 是否包含 Agent 初始化配置 | 包含 `sta100-release/config/sta100-agents.json` |
| 是否包含启动脚本 | 包含 `sta100-release/scripts/sta100.sh` |
| 是否存在运行产物 | 当前 release 目录存在 `data/*.db`、`auth.json`、`logs/sta100-web.log`，需确认最终包是否清理 |

## 七、建议后续处理顺序

1. 先确认部署包策略：是否允许带空 `data/auth.json`、空 SQLite、日志文件；是否必须在目标机器预装 Node。
2. 处理 Agent 后端并发队列：统一限制最多 2 个运行，增加排队状态。
3. 更新文档口径：模型数量、模板中心、本地客户发现单选/多选、Node 前置条件。
4. 将 `501 TODO_*` 清单转成客户确认表，逐项收集业务资料。
5. 修复浏览器视觉测试脚本，补一键启动 headless Chrome 的检测脚本。
6. 确认 OpenClaw workspace 历史输出和 backups 是否清理或加入忽略策略。

