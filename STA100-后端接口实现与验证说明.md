# STA-100 后端接口实现与验证说明

更新日期：2026-08-12  
代码目录：`/home/User/gsx/gitdir/std/sta100-web`  
实现语言：Go 1.20+  
持久化：SQLite（纯 Go 驱动）

## 1. 状态与数据边界

| 标记 | 含义 | 接口行为 |
|---|---|---|
| 已实现 | 已有 Go 逻辑、SQLite 或本机 OpenClaw 调用，前端已接入 | 成功返回 2xx，失败返回明确错误码 |
| 部分实现 | 可完成本地业务步骤，但外部抓取、模型生成或正式文件尚缺资料 | 响应中明确 `partial/todo`，不宣称完整完成 |
| 待客户资料 | 不能在缺少业务格式、来源或安全规范时可靠实现 | 返回 `501` 和 `TODO_*`，列出缺少项 |
| 外部依赖 | 代码链路已实现，但运行结果依赖本机 OpenClaw、模型账户或系统环境 | 可用时 2xx；不可用时 `503/504`，不生成假结果 |

空 SQLite 首次启动会写入客户、产品、报价、订单、单据、供应商、文件元数据、新闻、推荐、任务和插件的演示种子，`GET /api/v1/bootstrap` 返回 `dataStatus=seeded_demo_data`。这些记录只用于接口和页面验收，不是客户正式业务数据。前端不再以 JavaScript 静态业务数组作为失败回退。

真实数据分为四类：

1. SQLite 业务记录：页面 CRUD 的真实持久化结果。
2. 本机文件：客户上传至私有目录的原始文件，接口不暴露服务器绝对路径。
3. OpenClaw 数据：本机 Gateway、模型、凭据配置状态和 Agent 清单。
4. 待接数据：客户原始知识数据、OEM 工厂库、互联网来源、模板、OCR 和升级包规范，当前不得推断或伪造。

智能查询采用统一来源策略：页面不再让用户选择“本地/联网/RAG”。Go 先检索本地结构化证据，再交给系统 Agent 和业务 Agent 整合；响应仍保留来源、记录编号、时间、冲突和每个处理阶段状态。私有文件正文解析与联网工具未接入前，不得声称已读取正文或完成联网搜索。

## 2. 通用接口约定

- 除 `/api/health` 和登录/恢复接口外，`/api/v1/*` 均要求有效的 HttpOnly 会话 Cookie。
- `POST/PATCH/PUT/DELETE` 要求请求头 `X-STA100-Request: 1`，缺少时返回 `403 WRITE_GUARD_REQUIRED`。
- JSON 写请求使用 `Content-Type: application/json`；文件上传使用 `multipart/form-data`。
- 常用状态码：`200` 查询/更新成功，`201` 创建成功，`202` 请求已记录，`400` 参数错误，`401` 未登录，`403` 写保护，`404` 不存在，`409` 状态或引用冲突，`413/415` 文件限制，`501` 待资料，`503/504` 外部依赖不可用或超时。
- SQLite 默认路径：`~/.local/share/sta100/sta100.db`，环境变量 `STA100_DB_PATH` 可覆盖。
- 私有文件默认目录：`~/.local/share/sta100/private-files`，环境变量 `STA100_PRIVATE_DATA_DIR` 可覆盖。
- 账户文件默认路径：`~/.config/sta100/auth.json`，环境变量 `STA100_AUTH_FILE` 可覆盖。
- 业务写操作记录 `audit_logs`；API Key、密码、Cookie、完整聊天内容和私有文件绝对路径不进入接口响应。

## 3. 登录与基础服务

| 方法与路径 | 状态 | 实现逻辑与作用 | 关键校验/返回 | 前端与验证 |
|---|---|---|---|---|
| `GET /api/health` | 已实现 | 返回 Go 服务存活状态、服务名、时间和 `prototype=false` | 不含业务和密钥数据 | 部署探针；联调 200 |
| `GET /api/v1/auth/status` | 已实现 | 读取当前设备用户名并检查会话 Cookie | 不返回哈希或密码 | 登录页初始化；自动化覆盖 |
| `POST /api/v1/auth/login` | 已实现 | 校验本机账户，创建服务端 12 小时会话 | 默认首次账户 `admin/admin`；Cookie 为 HttpOnly、SameSite=Strict | 登录页；联调 200 |
| `POST /api/v1/auth/logout` | 已实现 | 删除服务端会话并清除 Cookie | 要求写保护头 | 侧栏退出 |
| `PATCH /api/v1/auth/account` | 已实现 | 验证当前密码后更新用户名和密码哈希 | 用户名/密码格式校验，账户文件 0600 | 设置-本机账户；自动化覆盖 |
| `POST /api/v1/auth/verify-master` | 已实现 | 仅验证设备维护密码 `sta100` | 不修改账户，不返回维护密码 | 忘记密码第一步 |
| `POST /api/v1/auth/reset` | 已实现 | 验证维护密码后重置当前用户名、密码并签发新会话 | 新密码重新加盐哈希 | 忘记密码第二步 |
| `GET /api/v1/bootstrap` | 已实现 | 一次读取业务表、文件元数据、新闻、推荐、任务、插件、偏好和概览聚合 | 返回 `schemaVersion`、`dataStatus`；不返回文件路径 | 登录后页面唯一业务初始化来源；单元/联调 200 |
| `GET /api/v1/search?q=` | 已实现 | 跨客户、订单、产品和私有文件元数据模糊搜索，最多 50 条 | 空关键字返回空数组 | 顶部全局搜索 |

## 4. 概览页面

| 方法与路径 | 状态 | 实现逻辑与作用 | 数据性质/客户待提供 | 前端与验证 |
|---|---|---|---|---|
| `GET /api/v1/overview` | 部分实现 | 聚合进行中订单、待复核单据、Agent 本地消息数、新闻、偏好和推荐 | SQLite 聚合真实；待办、日程来源待确认 | 概览初始化；联调 200 |
| `GET /api/v1/overview/summary` | 部分实现 | 返回六项概览计数；订单、单据、对话、新闻从库中计算 | `tasks/meetings=0` 并在 `todo` 标明来源待定 | bootstrap 同步使用 |
| `GET/PATCH /api/v1/overview/subscription` | 已实现 | 读取或保存日报推荐启用开关 | 本地设置 | 概览订阅开关 |
| `GET/PATCH /api/v1/preferences/recommendation` | 已实现 | 保存开关、频率、国家、主题、来源 | 频率仅 1/2/3/6/8/12/24 小时 | 概览/新闻设置 |
| `GET /api/v1/recommendations` | 已实现读取 | 读取 SQLite 中的推荐缓存 | 首次为演示种子；真实刷新来源待确认 | 概览推荐列表 |
| `GET /api/v1/overview/recommendations` | 已实现读取 | 上述推荐查询的页面别名 | 同上 | 兼容页面调用 |
| `POST /api/v1/overview/customers/search` | 历史兼容接口 | `local` 模式在客户档案按编号、名称、国家、类型和所有联系方式模糊检索，可要求必须有联系方式 | `rag/mixed` 仍返回 `501 TODO_RAG_DATA`；新页面不再提交来源模式 | 保留兼容；当前页面改用 `/assistant/query` |
| `GET /api/v1/overview/customers/{id}` | 已实现 | 读取本地客户详情 | 当前没有互联网字段级来源 | 预留详情调用 |
| `POST /api/v1/overview/oem/match` | 待客户资料 | 已保留查询、类目、排序和 Top N 入参入口，不返回示例工厂 | `501 TODO_OEM_DATA`；需工厂原始数据、类目、评分权重、TOP 口径、报告格式 | OEM 查询；自动化确认非 404 |
| `POST /api/v1/overview/oem-matches` | 待客户资料 | OEM 匹配兼容路径 | 同上 | 设计接口兼容路径 |
| `GET /api/v1/overview/oem-matches/{id}` | 待客户资料 | 工厂详情入口 | `501`；需认证、来源链接、抓取时间等字段 | 详情预留 |
| `POST /api/v1/overview/oem-matches/export` | 待客户资料 | 匹配报告导出入口 | `501`；报告格式和字段待确认 | 导出按钮显示明确警告 |
| `POST /api/v1/overview/customer-discovery` | 历史兼容接口 | 接收单选国家、城市、类型，旧入口保留明确 TODO | `501 TODO_DISCOVERY_DATA` | 当前页面改用 `/assistant/query` |
| `POST /api/v1/assistant/query` | 已实现统一链路 | 先检索客户、供应商、产品和私有文件元数据；Knowledge Agent 整理；按 `feature` 并行调用业务 Agent；Coordinator 汇总 | 消息最大 32 KiB；系统 Agent 不可由页面指定；冲突并列；失败返回 `partial=true`、证据及阶段状态 | OEM、客户统一搜索、本地客户发现和 Agent 聊天已接入；本地证据联调成功 |
| `GET /api/v1/agent-token-usage` | 已实现（临时测试） | 聚合 SQLite 中 OpenClaw 实际 usage，返回累计、本次、输入、输出、缓存、调用状态和按 Agent 统计 | 不保存提示词正文；usage 未返回的调用只计次数，不估算 Token | 顶栏 Token 入口和统计弹窗 |
| `DELETE /api/v1/agent-token-usage` | 已实现（临时测试） | 清空测试统计并写审计日志 | 不删除 Agent 会话、业务数据或 OpenClaw 配置 | 统计弹窗清空按钮 |

`/api/v1/assistant/query` 当前路由：OEM → `supplier-aggregator + brand-value-crawler`；客户搜索/发现 → `customer-measurement-agent + market-analyzer`；报价 → `export-agent + payment-advisor`；订单/物流 → `shipping-eta + inventory-agent`；单据 → `invoice-agent + export-agent`；产品 → `compatibility-agent + design-advisor`；新闻 → `market-analyzer + country-advisor`；直接聊天 → `context.targetAgent` 指定的业务 Agent。完整契约见《STA100-Agent分类与页面调用链路说明.md》。

## 5. 智能体与 OpenClaw

| 方法与路径 | 状态 | 实现逻辑与作用 | 关键边界 | 前端与验证 |
|---|---|---|---|---|
| `GET /api/v1/openclaw/status` | 外部依赖已接 | 限时读取版本、Gateway、RPC、systemd 和配置审计 | 不返回 token 或配置路径 | 设置系统信息 |
| `GET /api/v1/openclaw/models` | 外部依赖已接 | 读取默认模型、可用模型和凭据是否配置 | 不返回 API Key 明文/摘要 | 模型配置列表 |
| `PUT /api/v1/openclaw/models/default` | 外部依赖已接 | 调用编排组件设置默认模型 | 模型 ID 校验、45 秒超时 | 兼容独立接口 |
| `POST /api/v1/openclaw/models/auth` | 外部依赖已接 | API Key 通过 stdin 写入 OpenClaw | Key 不进入命令参数、日志或响应 | 兼容独立接口 |
| `GET /api/v1/openclaw/agents` | 外部依赖已接 | 读取 OpenClaw Agent 安全字段，并过滤 `main` 和 2 个系统 Agent | OpenClaw 实际 27 个；接口只返回 24 个业务 Agent | Agent 管理弹窗 |
| `POST /api/v1/openclaw/agents/sync` | 外部依赖已接 | 按清单幂等同步 2 个系统 Agent 和 24 个业务 Agent，再返回页面可见业务 Agent | 最长 10 分钟，不删除客户状态和清单外 Agent | Agent 管理同步 |
| `GET /api/v1/agents` | 外部依赖已接 | Agent 列表业务别名，仅返回 24 个业务 Agent | 系统 Agent 页面不可见 | 智能体页 |
| `GET /api/v1/agents/{id}` | 外部依赖已接 | 从真实 Agent 清单按 ID 返回详情 | 未注册返回 404 | 详情预留 |
| `POST /api/v1/agents/{id}/chat` | 兼容接口 | 校验 Agent、消息和会话，经编排组件调用真实 OpenClaw；成功后保存用户/助手消息 | CLI 90 秒、HTTP 105 秒；错误受控映射，不伪造回答 | 新页面聊天通过统一查询接口；本接口保留兼容 |
| `POST /api/v1/agents/chat` | 外部依赖已接 | 兼容旧聊天路径 | 同上 | 旧接口兼容 |
| `POST /api/v1/agents/weekly-report` | 部分实现 | 聚合 7 日审计日志和本机 Agent 消息，生成本地 Markdown | 返回 `partial=true`、`generationMode=local_aggregate`；客户确认模板后再接周报 Agent/PDF | 周报按钮和 Markdown 下载 |
| `POST /api/v1/agent-backups` | 已实现 | 将 `~/.openclaw/agents` 归档为 0600 tar.gz | 当前只创建备份，`restoreSupported=false` | 设置-数据安全 |

## 6. 客户、产品与供应商

| 方法与路径 | 状态 | 实现逻辑与作用 | 关键校验 | 前端与验证 |
|---|---|---|---|---|
| `GET /api/v1/accounts` | 已实现 | 关键字、类型、国家、分页以及订单数/累计金额/更新时间排序；订单数和金额从非取消订单动态聚合 | 默认不返回归档客户 | 客户列表；联调 200 |
| `POST /api/v1/accounts` | 已实现 | 创建客户并生成 `ACC-*` 编号 | 名称、国家必填；名称不区分大小写去重 | 新建客户；联调 201 |
| `GET /api/v1/accounts/{id}` | 已实现 | 返回客户及实时订单聚合 | 不存在/已归档返回 404 | 客户详情 |
| `PATCH /api/v1/accounts/{id}` | 已实现 | 更新客户；改名事务内同步报价、订单、单据的客户名称快照 | 名称唯一；引用同步全成或全退 | 编辑客户；单元测试覆盖改名 |
| `DELETE /api/v1/accounts/{id}` | 已实现 | 软归档客户，保留历史业务记录 | 后续新业务不能选择归档客户 | 删除/归档按钮 |
| `GET /api/v1/accounts/{id}/relations` | 已实现 | 一次返回客户、报价、订单和单据关系 | 按客户名称快照关联 | 客户详情标签；自动化覆盖链路 |
| `GET /api/v1/accounts/{id}/communications` | 已实现 | 按发生时间和创建时间倒序返回客户全部沟通历史 | 客户归档后仍可读取 | 客户详情与编辑；单元测试覆盖 |
| `POST /api/v1/accounts/{id}/communications` | 已实现 | 追加沟通方式、主题、内容、联系人和发生时间；服务端写入编号、创建时间和当前登录用户 | 必填方式/内容/发生时间；归档客户返回 409；不提供 PATCH/DELETE | 新增沟通记录；单元测试验证不可变与归档保留 |
| `GET /api/v1/accounts/export` | 已实现 | 动态聚合后导出真实 XLSX，记录审计 | 排除归档项；内部 ZIP/XML 可解析 | 导出按钮；单元测试解压并解析 XML |
| `POST /api/v1/accounts/ocr` | 待客户资料 | 名片/照片 OCR 入口 | `501 TODO_OCR_SPEC`；需 OCR 引擎、样例、字段和置信度阈值 | 客户图片识别显示警告 |
| `GET/POST /api/v1/products` | 已实现 | 查询或创建产品主数据 | 编码、名称、HS Code 必填；库存/价格非负 | 产品库列表和新建 |
| `GET/PATCH/DELETE /api/v1/products/{id}` | 已实现 | 详情、更新、停用；被报价/订单/单据引用时拒绝停用 | 引用冲突返回 409 | 产品详情/编辑/停用；单元测试覆盖 409 |
| `POST /api/v1/products/import` | 待客户资料 | 批量导入入口 | `501 TODO_PRODUCT_IMPORT_FORMAT`；需模板、必填字段、重复规则和错误回执 | 批量导入按钮 |
| `GET/POST /api/v1/suppliers` | 已实现 | 供应商查询、排序和创建 | 公司名称必填并去重 | 供应商列表/新建 |
| `GET/PATCH/DELETE /api/v1/suppliers/{id}` | 已实现 | 详情、更新和软归档 | 当前尚无产品/OEM 外键，后续接引用校验 | 供应商详情/编辑/归档 |
| `GET /api/v1/suppliers/export` | 已实现 | 导出真实 XLSX 并记录审计 | 内部 ZIP/XML 可解析 | 导出按钮；自动化覆盖 |

## 7. 报价、订单与单据

| 方法与路径 | 状态 | 实现逻辑与作用 | 关键校验/边界 | 前端与验证 |
|---|---|---|---|---|
| `GET /api/v1/quotes` | 已实现 | 关键字、状态、有效期范围、金额/更新时间排序 | 列表来自 SQLite | 报价列表；联调 200 |
| `POST /api/v1/quotes` | 已实现 | 校验客户和产品，按数量、单价、折扣计算行金额，再加运费和税费，创建 Draft | 客户必须有效；日期 `YYYY-MM-DD`；折扣 0-100 | 新建报价；联调计算 `360+20+5=385` |
| `GET/PATCH/DELETE /api/v1/quotes/{id}` | 已实现 | 详情、重算更新、受控归档 | 状态字典校验；已转订单不可删；仅 Draft/Rejected 可删 | 查看/编辑/归档 |
| `POST /api/v1/quotes/{id}/deliver` | 待客户资料 | 仅先校验报价存在且为 Draft，不修改为已发送 | `501 TODO_QUOTE_DELIVERY`；需模板、PDF、邮件样式、发件账户、收件规则和重试 | 当前页面未提供假发送成功；联调 501 |
| `GET /api/v1/quotes/{id}/download` | 待客户资料 | 报价 PDF 下载入口 | `501 TODO_QUOTE_PDF`；不再返回文本冒充 PDF | PDF 按钮显示警告；联调 501 |
| `POST /api/v1/quotes/{id}/convert-order` | 已实现 | 仅 Accepted 报价可转；折扣折入成交单价，复制产品快照并保留报价 ID | 同一报价只能转一次；库存校验 | 转订单；联调 201 |
| `GET /api/v1/orders` | 已实现 | 关键字、状态、交付日期、金额/更新时间排序 | SQLite 查询 | 订单列表；联调 200 |
| `POST /api/v1/orders` | 已实现 | 手工创建订单，读取产品库校验库存并计算总额 | 客户有效；产品启用；数量不超过库存；进度 0-100 | 新建订单 |
| `GET/PATCH/DELETE /api/v1/orders/{id}` | 已实现 | 详情、更新重算、受控归档 | 状态字典；已有单据时禁止删除 | 订单查看/编辑/归档 |
| `POST /api/v1/orders/{id}/documents` | 已实现业务草稿 | 先校验所有类型和订单，再按同一订单快照生成 PI/CI/PL/报关单 Review 记录 | 类型只允许四类；正式模板和文件输出仍待提供 | 一键全套；联调生成 4 条 |
| `GET /api/v1/documents` | 已实现 | 按关键字、类型、状态查询 | 关键字覆盖编号、客户、订单、模板 | 单据列表与组合筛选 |
| `POST /api/v1/documents` | 已实现业务草稿 | 按订单生成单类单据记录，复制客户、金额和产品明细快照 | 类型和订单必须有效 | 新建单据 |
| `GET/PATCH/DELETE /api/v1/documents/{id}` | 已实现 | 查看、局部更新模板/语言/状态、归档 | 订单不可改；Confirmed/Sent/Cancelled 锁定；仅 Draft/Review 可删 | 详情/编辑/归档；联调局部 PATCH 200 |
| `GET /api/v1/documents/{id}/download` | 待客户资料 | 正式单据文件下载入口 | `501 TODO_DOCUMENT_EXPORT`；需四类模板、字段映射、格式、语言、签章和命名规则 | 下载按钮显示警告；联调 501 |

当前“生成单据”指生成可持久化、可复核的业务草稿和订单快照，不代表已经生成符合客户版式的 PDF/DOCX/XLSX。正式文件只在模板和字段规则确认后实现。

## 8. 私有数据库与新闻

| 方法与路径 | 状态 | 实现逻辑与作用 | 关键校验/边界 | 前端与验证 |
|---|---|---|---|---|
| `GET /api/v1/private-files` | 已实现 | 按关键字和分类查询文件元数据 | 响应不含绝对路径 | 数据库列表；联调 200 |
| `POST /api/v1/private-files/upload` | 已实现存储 | 最大 50 MB；校验扩展名；流式写入、SHA-256 去重、0600 权限、元数据入库 | 支持 PDF/DOCX/XLSX/CSV/TXT/MD/JPG/JPEG/PNG；状态为 `PendingParse` | 上传；单元/联调 201 |
| `GET/PATCH/DELETE /api/v1/private-files/{id}` | 已实现 | 读取/修改元数据；删除时同时删除受控私有目录中的文件并软删记录 | 文件名、分类必填；路径不能由客户端修改 | 详情/编辑/删除 |
| `GET /api/v1/private-files/{id}/content` | 已实现 | 从受控目录定位 ID 对应文件并以内联方式返回 | 无文件实体的演示元数据返回 404 | PDF/图片预览；联调内容一致 |
| `GET /api/v1/private-files/{id}/download` | 已实现 | 下载本机原始文件 | 不接受客户端路径 | 下载按钮 |
| `POST /api/v1/private-files/{id}/reindex` | 部分实现 | 将状态改为 `WaitingDataFormat` 并记录重建请求 | 返回 202；尚未执行解析/向量索引 | 重新索引按钮 |
| `GET /api/v1/private-files/{id}/summary` | 待客户资料 | 文件摘要入口 | `501 TODO_PRIVATE_FILE_PARSER`；需格式、解析器、摘要字段和索引策略 | 摘要按钮；联调 501 |
| `GET /api/v1/news` | 已实现读取 | 按分类和数量读取新闻缓存 | 首次为演示种子，不是实时联网结果 | 行业新闻列表 |
| `GET /api/v1/news/{id}` | 已实现读取 | 读取单条新闻详情 | 同上 | 详情预留 |
| `POST /api/v1/news/refresh` | 待客户资料 | 新闻抓取/Agent 调度入口 | `501 TODO_NEWS_SOURCES`；需白名单、授权、频率、重试和留存周期 | 刷新按钮显示警告 |
| `POST /api/v1/tasks` | 待客户资料 | 新闻生成待办等入口 | `501 TODO_TASK_SOURCE`；需字段、提醒、完成规则和 Agent 同步范围 | 生成待办显示警告 |

## 9. 设置、插件、任务与升级

| 方法与路径 | 状态 | 实现逻辑与作用 | 关键边界 | 前端与验证 |
|---|---|---|---|---|
| `GET/PATCH /api/v1/settings/preferences` | 已实现 | 读取/保存新闻数量、频率、关注条件和每个 Agent 联网白名单 | 数量 1-100；频率受控；白名单由页面校验域名 | 新闻设置、Agent 白名单 |
| `GET/PATCH /api/v1/settings/model` | 外部依赖已接 | 使用与固定 OpenClaw `2026.7.1-2` 对应的 Go 模型目录（20 个 API Key 提供商、106 个文本模型），只调用 `models status` 获取默认模型与凭据状态；页面按提供商联动模型，保存时经 stdin 更新该提供商 Key，再设置默认模型 | 模型与 Key 提供商必须一致；排除 OAuth/CLI/本地兼容入口；本次输入可显示/隐藏，已保存 Key 不回传明文；升级 OpenClaw 时同步更新目录版本 | 设置页内联模型配置 |
| `POST /api/v1/settings/model/test` | 已实现真实模型验证 | 依次检查 OpenClaw Gateway/RPC、默认模型、提供商凭据，并通过 `sta100-coordinator` 发起最小真实生成请求；返回阶段、模型、耗时、响应和 Token usage | 只有收到真实模型响应才返回 `ok=true`；API Key、原始命令和敏感日志不进入响应 | 模型设置“真实可用性验证”状态行 |
| `GET/PATCH /api/v1/plugins` | 部分实现 | 读取插件记录并保存启用状态、能力和绑定状态 | 绑定凭据、推送内容和同步范围待确认；响应含 `todo` | 微信/飞书插件设置；单元测试 PATCH |
| `GET/POST/PATCH /api/v1/jobs` | 已实现配置 | 查询、创建或按请求体 ID 更新任务配置 | 内置任务不可删除；当前不含实际调度执行器 | 定时任务设置；单元测试 PATCH |
| `PATCH/DELETE /api/v1/jobs/{id}` | 已实现配置 | 更新任务或删除自定义任务 | 内置任务删除返回 409 | 定时任务编辑/删除 |
| `GET /api/v1/system/health` | 已实现 | 检查 SQLite、磁盘、私有文件统计、索引状态、Go 运行时和 OpenClaw | OpenClaw 不可用时整体 `degraded`，业务库仍可用 | 系统信息；联调 200/degraded |
| `GET /api/v1/system/upgrade/history` | 已实现空记录 | 返回升级审计列表结构 | 未执行升级时为空 | 升级记录 |
| `POST /api/v1/system/upgrade/import` | 待客户资料 | 离线包导入入口 | `501 TODO_UPGRADE_PACKAGE_SPEC`；需 manifest、签名、兼容、迁移和回滚规范 | 上传 `.zip` 后显示警告 |
| `POST /api/v1/system/upgrade/install` | 待客户资料 | 安装、健康检查、失败回滚和自动重启入口 | 同上；当前绝不模拟安装或重启 | 尚无可执行安装按钮 |
| `GET /api/v1/templates` | 部分实现 | 返回空模板列表和 `waiting_spec` | 正式模板待提供 | 模板中心 |
| `POST /api/v1/templates/upload` | 待客户资料 | 模板文件上传入口 | `501 TODO_TEMPLATE_SPEC` | 模板文件按钮 |
| `POST /api/v1/templates/image-recognition` | 待客户资料 | 图片 OCR/版式识别入口 | `501`；需 OCR 引擎、置信度和校正规则 | 图片生成模板按钮 |
| `GET/PATCH /api/v1/templates/{id}` | 待客户资料 | 模板详情和人工映射入口 | GET 当前空结构/不存在语义后续完善，PATCH 返回 501 | 模板维护入口 |
| `POST /api/v1/templates/{id}/publish` | 待客户资料 | 模板不可变版本发布入口 | `501`；发布和默认版本规则待确认 | 接口预留 |

## 10. 已执行验证

### 10.1 自动化与静态检查

```bash
gofmt -w *.go
go test ./...
go vet ./...
node --check assets/app.js
```

结果：全部通过。测试覆盖 bootstrap、客户 CRUD/改名引用同步、不可变客户沟通记录、服务端报价计算、报价转订单、全套单据、库存不足、产品引用冲突、客户/供应商 XLSX 解压与 XML 解析、任务/插件集合 PATCH、私有文件上传/内容读取/摘要 TODO、系统/业务 Agent 过滤及统一查询相关路由，以及设计中的 TODO 路径不会退化成 404。

### 10.2 临时环境接口联调

联调使用独立环境变量和 `/tmp` 文件，不读取或修改正式业务库：

```bash
STA100_DB_PATH=/tmp/sta100-integration-20260812.db \
STA100_AUTH_FILE=/tmp/sta100-auth-20260812.json \
STA100_PRIVATE_DATA_DIR=/tmp/sta100-private-20260812 \
go run . -addr 127.0.0.1:18080
```

| 验证链路 | 结果 |
|---|---|
| 未登录 bootstrap | `401` |
| `admin/admin` 登录 | `200` |
| bootstrap、客户、报价、订单、单据、产品、供应商、文件、概览、新闻、任务、插件、健康 | 全部 HTTP `200` |
| 写请求缺少 `X-STA100-Request` | `403` |
| 创建客户 | `201`，生成 `ACC-*`，累计金额为 0 |
| 追加/查询客户沟通记录 | `201/200`；PATCH/DELETE 返回 405；客户归档后历史可读、新增返回 409 |
| 创建报价 | `201`，2 x 200 x 90% + 20 + 5 = `EUR 385.00` |
| 报价发送/报价 PDF | 均 `501`，返回具体缺少项且未改状态 |
| 接受报价并转订单 | `200/201`，保留报价 ID 和产品快照 |
| 订单生成全套单据 | `201`，生成 PI、CI、PL、报关单四条 Review 记录 |
| 单据只更新模板字段 | `200`，类型、订单、状态保持不变 |
| 单据下载 | `501 TODO_DOCUMENT_EXPORT` |
| 私有 MD 文件上传/在线读取 | `201/200`，内容逐字一致，响应不泄露绝对路径 |
| 私有文件摘要 | `501 TODO_PRIVATE_FILE_PARSER` |
| 系统健康 | `200`；本次外部 OpenClaw 检查不可用时状态为 `degraded`，SQLite 接口不受影响 |
| Agent 页面接口 | 返回 24 个业务 Agent；不包含 `main`、`sta100-knowledge`、`sta100-coordinator` |
| 统一智能查询 | 本地检索阶段正常，返回证据、结构化记录和完整 pipeline；模型失败时 `partial=true` 且保留本地结果 |

真实模型联调已到达 DeepSeek 提供商，但 `deepseek/deepseek-v4-flash` 返回账户余额不足。Knowledge Agent、业务 Agent 和 Coordinator 均按失败阶段记录，接口未伪造模型答复。恢复余额后仍需补做完整模型内容和冲突并列质量验收。

## 11. 后续接入顺序

1. 客户确认原始数据目录、格式样例、增量标识、分类、字段映射和保存周期后，实现解析、摘要、增量索引和 RAG。
2. 客户提供 OEM 工厂数据、骑行类目、评分权重、排序口径和报告样式后，实现匹配、详情和导出。
3. 客户确认互联网/新闻来源白名单、授权、抓取频率、留存和去重后，实现联网检索和新闻刷新。
4. 客户提供报价、PI、CI、PL、报关单模板及邮件样式、签章、语言和文件命名规则后，实现正式文件与发送历史。
5. 客户确认 OCR 引擎、样例和阈值后，实现名片识别和图片模板识别。
6. 客户提供离线升级包 manifest、签名公钥、兼容矩阵、迁移、回滚和重启托管方式后，实现离线升级闭环。
7. 客户确认待办、日程、插件绑定和通知同步规则后，实现真实调度、微信/飞书绑定和概览统计。
