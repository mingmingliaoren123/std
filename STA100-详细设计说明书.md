# STA-100 骑行行业智能工作台详细设计说明书

版本：V1.2（页面交互完善、供应商与 OpenClaw 编排）  
日期：2026-08-11  
适用范围：`/home/User/gsx/gitdir/std/sta100-web`

## 1. 设计结论

本设计以最新的《功能梳理.txt》为准，并结合以下资料完成：

- `梳理文件/用户需求规格说明书-STA100-Go重构.md`
- `梳理文件/现有功能真假对照表.md`
- `梳理文件/客户待确认事项汇总-开发版.md`
- `梳理文件/客户指定输出格式整理.md`
- `需要的文件/总体原则.txt` 与 `总体原则.docx`
- `需要的文件/Donald-骑行智能体核心方向-20260613-v11j.pdf`
- `需要的文件/PI-20260625-DW01.pdf`
- `需要的文件/STRATRONIX-客户订单产品报价-架构文档-20260809.xlsx`
- 现有页面 `cycling-agent/web_templates/dashboard.html`、`web_static/css/dashboard.css` 及配套脚本

本次交付是“可直接运行的页面实现 + OpenClaw 本机部署 + 独立 Go 编排组件 + 可落地的详细设计”。客户、报价、订单、单据、产品、文件和新闻仍为原型数据；OpenClaw `2026.7.1-2`、Gateway、模型状态和 24 个 Agent 已真实部署，并由 `openclaw-orchestrator` 管理。后续仍需实现 SQLite 业务库和各页面业务接口，不得把页面演示记录作为真实业务事实。

### 1.1 当前已实现边界

| 范围 | 当前状态 | 真实性 |
|---|---|---|
| Go Web 服务 | 已实现静态资源内嵌、健康检查、安全响应头和 STA-100 OpenClaw HTTP 适配接口 | 真实运行 |
| Go 编排组件 | 独立 package、CLI、HTTP API、模型/凭据管理和通用 Agent manifest 同步 | 真实运行 |
| OpenClaw | 官方稳定版 `2026.7.1-2`，独立 Node.js 22.22.3 ARM64，user systemd 常驻 | 真实运行 |
| 模型设置 | 读取模型列表/默认模型/凭据状态，可切换默认模型，可经 stdin 更新 API Key | 真实配置 |
| Agent 管理 | `main + 24` 已注册，24 个业务 Agent 有独立工作区、名称和 Emoji，可按清单幂等同步 | 真实配置 |
| 单据筛选 | 支持编号、客户、订单、模板搜索，PI/CI/PL/报关单类型和状态组合过滤 | 前端真实交互，数据为原型 |
| 业务主数据 | 客户、报价、订单、单据、产品、供应商的前端 CRUD、关系模糊搜索、详情跳转、筛选和排序可操作 | 前端内存状态，数据为原型 |
| 其它页面 | 页面、弹窗、抽屉、筛选和前端业务流程可操作 | 数据和多数操作为原型 |

## 2. 范围与取舍

### 2.1 最新页面顺序

左侧导航固定为：

1. 概览
2. 智能体
3. 客户
4. 报价单
5. 订单
6. 单据
7. 产品库
8. 供应商
9. 数据库
10. 行业新闻
11. 设置

保留现有页面的暗色工作台、侧边导航、顶部搜索、状态提示、卡片、表格、抽屉和多语言入口。OEM 工厂智能匹配、客户统一搜索和本地客户发现保留在概览，但不增加三个独立主导航入口；客户开发和今日日程仍按现有业务页面归并。

### 2.2 旧能力归并

| 旧能力 | 新归属 | 处理方式 |
|---|---|---|
| 今日待办、今日会议、今日处理文档、进行中订单、今日对话、行业资讯 | 概览 | 作为日报订阅下的六个摘要入口，点击打开明细 |
| 为你推荐、行业日报 | 概览 + 行业新闻 | 推荐保留在概览，行业新闻独立页面展示三条热点并支持 MORE |
| OEM 工厂匹配 | 概览业务工具 | 页面保留查询、类目、排序类别、Top 数量、详情和报告导出；正式数据分类和评分规则保留 TODO |
| 客户统一搜索、本地客户发现 | 概览业务工具 | 统一搜索支持本地/RAG/混合及联系方式过滤；本地发现支持国家/城市/类型单选并调用客户洞察 Agent |
| 客户开发 | 客户页面/客户洞察智能体 | 客户列表维护正式档案，智能体提供公开信息分析；不再作为独立首页模块 |
| 报价、订单、产品、单据 | 四个独立页面 | 使用客户中心关系模型互相引用 |
| 供应商 | 产品库同级页面 | 维护公司、联系方式、产品、规格、报价、备注和来源，后续与 OEM 结果关联 |
| 私有数据库、RAG 检索 | 数据库 + 智能体来源面板 | 数据库维护文件，智能体对话展示本地知识库、私有库、联网检索 |
| 恢复备份 | 移除 | 最新需求明确不要恢复备份 |
| 立即备份 | 设置/数据库 | 只备份用户操作的智能体数据，不默认覆盖客户业务数据 |
| CO 产出 | 移除 | 单据类型改为 PI、CI、PL、报关单 |
| 微信小程序单独绑定 | 设置插件绑定 | 与 OpenClaw 插件绑定保持一致，首期展示微信、飞书 |

## 3. 总体信息架构

```text
STA-100 工作台
├─ 概览
│  ├─ 日报订阅与六项今日摘要
│  ├─ 为你推荐
│  └─ 行业新闻三条热点
├─ 智能体
│  ├─ 24 个 OpenClaw Agent
│  ├─ 专业分类与快捷问题
│  ├─ 对话（本地知识库 / 私有 / 联网检索）
│  └─ 全智能体周报
├─ 业务关系中心
│  ├─ 客户
│  ├─ 报价单
│  ├─ 订单
│  ├─ 单据
│  └─ 产品库
├─ 私有知识
│  └─ 数据库
├─ 行业情报
│  └─ 行业新闻
└─ 系统管理
   ├─ 模型设置
   ├─ 插件绑定
   ├─ 定时任务
   ├─ 智能体备份
   ├─ 数据安全
   ├─ 系统信息
   └─ 版本升级
```

### 3.1 核心关系

```text
客户 1 ── N 报价单 ── 0/1 订单 ── N 单据
客户 1 ── N 订单
产品 N ── N 报价单明细 / 订单明细 / 单据明细
文件 N ── N 标签，文件 1 ── 1 主分类
智能体 1 ── 1 OpenClaw Agent ── N 会话与操作记录
```

业务链：选择客户 → 选择产品 → 生成报价单 → 客户接受 → 转订单 → 用订单快照生成 PI/CI/PL/报关单。已锁定的订单和单据不受产品价格后续变化影响。

## 4. 数据来源与真实性口径

页面必须在真实版本中显示来源标签，并按以下顺序处理：

| 来源 | 作用 | 是否可直接作为业务事实 | 页面标识 |
|---|---|---|---|
| 本地业务 SQLite | 客户、报价、订单、产品、单据、操作记录 | 是，以数据库当前记录为准 | 本地业务数据 |
| 客户私有数据库 | 客户上传的文件、OCR 结果、分类、标签、索引 | 是，但需标识客户私有 | 客户私有数据 |
| 出厂通用资料 | 预置产品手册、行业知识、模板、法规资料 | 是，按资料版本和更新时间判断 | 出厂通用资料 |
| RAG 检索结果 | 从已索引资料返回证据片段 | 可引用，不应无来源改写成确定事实 | RAG 来源 |
| 联网检索 | 推荐、行业新闻、市场情报、客户公开信息 | 需展示来源、获取时间和原文链接 | 联网检索 |
| OpenClaw / LLM | 对已取得数据进行理解、归纳、生成建议 | 不是数据源本身，不能替代业务数据库 | 模型生成 |
| 前端原型数据 | 当前目录中页面演示 | 不可作为验收业务事实 | 原型数据 |

关键原则：本地业务数据和本地/私有文件的检索应由 Go 代码和 SQLite 索引先完成，不因为文件量大而把全量内容塞给模型；OpenClaw Agent 只接收必要的检索结果与用户问题。回答同时展示本地知识库和联网检索时，必须分组展示，避免客户误以为网络信息属于本地事实。

## 5. 页面详细设计

### 5.1 全局壳层

#### 视觉与布局

- 采用截图对应的浅色中性主题：页面工作区 `#f7f7f9`、内容面 `#ffffff`、正文 `#202124`、边框 `#d9dce2`。
- 粉色 `#ed3f93` 用于品牌、主按钮和当前导航；蓝色 `#2378ed` 用于链接、选择和次要交互；绿色 `#2ca56d` 只表示在线、正常和已完成；琥珀色与红色分别表示待处理和错误风险。
- 桌面端固定左侧导航，内容区最大宽度 1680px；移动端导航变为抽屉。
- 顶栏显示当前页面、全局搜索、刷新、通知、语言切换。
- 工具按钮使用本地 Lucide 图标；品牌和概览业务工具复用原应用图片资产；顶级模块和 24 个 Agent 同时复用原应用 Emoji。所有脚本和图片均本地提供，不依赖 CDN。
- 页面区块使用扁平面板，重复记录才使用卡片；表格支持横向滚动，不压缩字段导致数据重叠。
- 登录、弹窗、抽屉、命令搜索、智能体聊天和移动导航使用同一套浅色变量；新闻图片内允许保留局部暗色遮罩，单据预览保留纸张自身配色。

#### 全局交互

| 交互 | 页面表现 | 正式实现要求 |
|---|---|---|
| 页面切换 | 左侧导航高亮、URL hash 更新、回到页面顶端 | Go 返回统一壳层或服务端路由，保留深链接 |
| 全局搜索 | Ctrl/Cmd+K 打开命令搜索，按客户/订单/产品/文件聚合 | `/api/v1/search?q=&types=`，结果必须带类型、ID、跳转目标 |
| 返回 | 抽屉/弹窗关闭返回原列表；浏览器后退不丢列表筛选 | 正式版用 URL query 保存筛选和分页 |
| 状态 | 顶栏、设置页展示服务器、数据库、模型、OpenClaw 状态 | `/api/v1/system/health`，异常显示错误码和重试入口 |
| 语言 | 中文/英文切换，首期不承诺 17 种全部完成 | UI 字符串走 i18n，客户业务原文不自动覆盖 |
| 错误 | Toast 只提示结果，表单错误显示在字段旁 | 后端错误统一 `code/message/details/request_id` |

### 5.2 概览

#### 页面结构

1. 日报订阅横幅：保留开关；开启后按新闻设置中的 1/2/3/6/8/12/24 小时频率获取推荐信息。
2. 六个今日摘要按钮：今日待办、今日会议、今日处理文档、进行中订单、今日对话、行业资讯。
3. “为你推荐”：默认 3 条，可展开到 5 条/配置数量；每条显示标题、摘要、来源、类型、时间和详情入口。
4. 行业新闻：固定显示 3 条热点，MORE 跳转行业新闻页。
5. OEM 工厂智能匹配：按骑行类目和查询条件筛选工厂，页面来源统一为本地知识库、联网检索两类；RAG 检索和公开网络采集属于联网资料处理链路，不作为两个并列页面来源。支持按匹配度、产能、MOQ、数据来源排序，Top 3/5/10，查看详情和导出报告。类目、评分权重和正式报告字段待客户提供后实现。
6. 客户统一搜索：支持本地知识库搜索（默认）、联网检索、本地知识库 + 联网检索；支持“必有联系方式”过滤，联系方式包含邮箱、电话、网站及客户确认的其它通信方式。
7. 本地客户发现：国家、城市、客户类型各只能选择一个；查询条件提交后由 `CustomerMeasurementAgent` 读取本地数据和允许的公开数据，再返回统一客户字段、联系方式、匹配分和返回理由。

#### 实现逻辑

- 订阅开关写入 `user_preferences.recommendation_enabled`；Go scheduler 每 60 分钟调用 recommendation job。
- 推荐 Agent 从用户配置的国家、城市、类型、关键词、来源白名单读取条件；先查询本地和 RAG 相关内容，再按允许来源获取互联网信息，去重后按相关度、时间、新颖度排序。
- 六项摘要分别聚合智能体操作日志、会话记录、文档任务、订单表和新闻推荐表；不由模型直接编造数量。
- “行业资讯”必须引用推荐记录；“进行中订单”必须引用订单表。
- 三项业务工具当前原型使用标记为 prototype 的示例数据，正式接口不能把示例结果当成真实结果。Go 服务应按查询条件记录 `request_id`，分别调用本地业务库、私有 RAG 检索器和互联网来源提供器，并为每个字段保留 `source_type、source_uri、retrieved_at`。
- OEM 链路为：输入类目/数量/要求 → 本地工厂库查询 + 外部资料获取（RAG 检索/互联网白名单采集）→ 字段归一化 → 规则评分 → 选择排序字段和 Top 数量 → 页面按本地/外部来源展示并导出报告。客户未提供类目和评分规则前仅保留 TODO。
- 统一搜索链路为：输入关键字 → 按模式选择本地 SQL、RAG，或两者并行 → 统一客户字段 → 按联系方式存在性过滤 → 去重排序 → 按来源分组展示。
- 本地客户发现链路为：单国家 + 单城市 + 单类型 → 本地客户数据查询 → 允许的公开数据补充 → `CustomerMeasurementAgent` → 结构化结果校验 → 展示及详情追溯。

#### 待确认 TODO

- 推荐互联网网站/平台白名单、访问授权、抓取失败重试策略。
- “待办”和“会议”正式来源是智能体日志、用户手动录入，还是二者并集。
- 推荐的默认数量、保存周期和推送渠道。

### 5.3 智能体

#### 页面结构

- 顶部模型状态提示；模型未配置时，所有卡片显示“待配置”并提供进入设置快捷入口。
- 24 张 Agent 卡片，分贸易与出口 8、门店与产品 6、市场分析 4、客户与支持 6。
- 每张卡显示中文名称、技术映射名、职责、三个热点快捷问题、可用状态。
- 顶部提供智能体管理和生成本周周报。
- 对话区将本地业务数据库、客户私有知识库、联网检索显示为可勾选来源；联网检索旁提供当前 Agent 的域名白名单管理。

#### Agent 链路

```text
用户输入
  → Go /api/v1/agents/chat
  → 校验 Agent ID、会话键、消息长度和来源选项
  → 将来源选项和联网域名白名单写入临时 0600 消息文件
  → openclaw agent --agent --message-file --session-key --json
  → OpenClaw Gateway 调用对应 Agent
  → Go 解析 runId/status/result.payloads.text
  → 页面分组展示用户消息、真实回答、来源和失败重试
```

每个页面智能体与一个 OpenClaw Agent 一一映射。当前部署以 `sta100-web/config/sta100-agents.json` 为业务声明清单，由 `openclaw-orchestrator` 通过官方 `agents add` 和 `agents set-identity` 创建/校正；Go 只映射 `agents.list` 中的 ID、名称、Emoji 和模型字段，不返回配置文件其它内容。后续业务库中的 `agent_registry` 保存页面映射、技能、来源权限和启停状态，不能替代 OpenClaw 自身 Agent 配置。

周报使用所有 Agent 的操作记录和会话统计，不把“用户点击卡片”误算为有效业务成果；至少输出使用量、重要对话、完成事项、待跟进、来源摘要五部分，格式沿用客户指定 Markdown 报告规则。

#### 待确认 TODO

- 24 个 Agent 最终中文名称、初始化提示、技能目录、分类描述、来源权限和是否允许互联网访问。
- OpenClaw Gateway 的最终流式输出、取消和审计字段仍待后续补充；当前同步调用超时 105 秒，CLI 超时 90 秒，模型错误和额度错误返回明确失败状态。

### 5.4 客户

#### 列表

支持关键字搜索、客户类型、国家筛选、排序、分页、批量选择、Excel 导出、详情、编辑。默认展示客户名称/编号、类型、国家、联系人、订单数、累计金额、评级和更新时间；字段选择器控制列显隐。

#### 新建与详情

- 新建支持图片/名片识别和手动录入；识别结果必须人工确认后才能保存。
- 基础字段按架构表设计：负责人、客户名称、自动编号、客户类型、行业、评级、电话、网站、所有权、员工数、账单地址、收货地址、LinkedIn、描述、标签、备注。
- 客户名称、客户类型、主电话、账单国家为首期建议必填；最终必填项 TODO。
- 订单数量和累计金额为系统依据关联订单自动计算，不允许客户手填；详情页查看全字段。
- 删除采用软删除；存在报价、订单、单据、产品引用时禁止硬删除，只允许归档。

#### 关联动作

- 客户详情可创建报价单；新报价继承客户、联系人和地址快照。
- 报价接受后复制明细生成订单，保留 `source_quote_id`。
- 订单可生成单据，单据保存订单和客户快照，避免后续客户信息变化破坏历史凭证。

### 5.5 报价单

#### 页面功能

列表/看板切换、搜索、状态筛选、有效期筛选、新建、详情、编辑、PDF 导出、模板管理。

#### 字段与计算

报价负责人、主题、报价编号、状态、有效期、客户、联系人、商机（预留）、币种、明细行、小计、折扣、税、调整、运费、总计、账单地址、收货地址、条款与条件、备注。总计由明细行和费用自动计算，草稿状态允许重算。

#### 状态

`Draft → Delivered → Accepted / Rejected → Invoiced/Archived`。发送后记录发送历史；接受后提供转订单；拒绝必须填写原因；进入锁定状态后禁止修改明细。

#### 模板

支持固定模板、自定义模板、上传报价单文件、上传图片识别模板。图片识别输出字段坐标和置信度；字段置信度不足进入待人工校正，不直接用于正式报价。

当前页面已将“上传图片生成模板”和“上传模板”拆分：图片接受 JPG/PNG/WebP，文件模板接受 DOCX/XLSX/PDF/HTML，均先做本地格式和 20 MB 大小校验并明确显示“等待后端接口”。上传、OCR、字段映射、人工校正和发布接口尚未实现，原因是正式模板样例、占位符规范、OCR 引擎及精度、纸张/语言/签章规则尚未提供；不得将本地文件选择显示为上传成功。

### 5.6 订单

#### 页面功能

列表、状态筛选、交付日期筛选、关键字搜索、新建、详情、编辑、生成单据、模板管理。

#### 规则

- 新建订单默认是本系统订单草稿，不自动进入第三方网站，不支持未确认的真实下单。
- 订单来源可以是报价单转换或手动创建；转换复制客户、联系人、地址和报价明细。
- 页面原型已将报价和订单明细关联至同一产品库：只能选择启用产品，显示库存，支持多行、数量、单价、小计和总额联动；订单保存时校验数量不超过当前库存。
- 明细保存 `product_id、name_snapshot、quantity、unit_price、amount`；报价转订单复制成交价快照，订单生成单据复制订单快照，产品库后续改名或改价不反向修改历史业务记录。
- 订单状态首期：`Draft / Confirmed / Production / Shipped / Completed / Cancelled`；取消必须填写原因。
- 订单金额、交付进度来自订单自身记录；与第三方平台实时同步需要客户后续确定平台、登录授权、同步频率和冲突处理，当前 TODO。
- 订单进入生产或发运后，关键字段锁定；修改通过变更记录处理。

### 5.7 单据

#### 页面功能

支持 PI、CI、PL、报关单四种单据，支持单个生成和一键生成全套；列表、搜索、筛选、详情预览、下载、模板上传/更新、状态维护。

#### 生成链路

```text
选择订单
  → 读取客户快照、订单明细、产品 HS CODE/材质/用途
  → 按单据类型选择模板版本
  → 字段校验（缺失/格式/金额一致性）
  → 生成可编辑草稿
  → 人工复核
  → 锁定并生成 PDF/指定格式
```

单据状态：`Draft / Review / Confirmed / Sent / Cancelled`。生成失败时保留失败记录和明确字段错误。正式单据的保存周期、电子签章、邮件样式、报关单字段和导出格式属于 TODO。

### 5.8 产品库

支持卡片/表格切换、搜索、类别筛选、库存排序、批量导入、新建、编辑、产品详情。字段包括产品名、产品编码、类别、制造商、库存、HS CODE、默认单价、销售价、描述、产品图、标签、启用状态、创建/修改记录。

产品状态为 `Review / Active / Inactive`；只有 Active 产品可加入新报价。产品被报价/订单/单据引用后不能直接删除。库存不足触发预警，库存数应来自本地业务库而非模型生成。

### 5.9 私有数据库

#### 分类与上传

- 页面展示主分类、文件数量、索引数、待人工确认数和存储量。
- 上传目标为设备本地客户私有数据区，不上传到第三方云盘。
- 处理链：扩展名和病毒/大小校验 → 文本提取或 OCR → 重复文件检查 → AI 分类建议 → 人工确认 → 标签写入 → 索引入库。
- 一个文件一个主分类、多个标签；分类和标签都可人工修正。
- 首期支持 PDF、DOCX、XLSX、CSV、TXT、MD、JPG、PNG；视频是否解析、支持哪些编码和是否只保存元数据需调研确认。

#### 浏览与维护

按分类进入文件列表，支持文件预览、下载、摘要、编辑分类、增删标签、删除/归档。对不支持在线预览的格式只能下载或查看 AI 摘要。全量扫描必须分为新增、更新、删除和未变化文件，避免重复消耗 Token；索引任务可暂停、重试和查看错误。

### 5.10 行业新闻

首页固定 3 条热点，MORE 进入独立页面。新闻详情展示标题、摘要、分类、来源、获取时间、相关度、原文链接和智能体摘要；默认离线缓存最近一次成功抓取内容。来源白名单、授权方式、失败重试和新闻留存周期 TODO。

### 5.11 设置

| 子项 | 页面内容 | 关键规则 |
|---|---|---|
| 模型设置 | OpenClaw 默认模型、可用模型、凭据提供商、API Key 更新 | 列表和状态真实读取；Key 留空表示保留，通过 stdin 写入且从不回显 |
| 插件绑定 | 微信、飞书及后续 OpenClaw 插件 | 不再单独设计微信小程序页面；绑定后的能力跟随插件定义 |
| 定时任务 | 内置推荐、周报、新闻、索引任务，自定义任务 | 内置任务不可删除，可编辑和开关；显示上次/下次/错误/重试 |
| 智能体备份 | 目录选择、备份、校验结果 | 只备份智能体配置、技能、会话、操作记录；不提供恢复按钮 |
| 数据安全 | 本地存储、密钥、最小化外发、操作记录 | 说明真实数据边界和脱敏策略 |
| 系统信息 | 设备、版本、OpenClaw、SQLite、连接、存储 | 对应健康检查结果，不显示假状态 |
| 版本升级 | 自动安装开关、离线包导入、风险提示 | 升级前校验、快照、失败回滚；数据不能丢失 |

## 6. SQLite 数据设计

以下为首期建议表，字段可用 `custom_fields` 扩展，所有业务表包含 `id、created_at、updated_at、deleted_at`。

| 表 | 关键字段 | 说明 |
|---|---|---|
| `accounts` | name、number、type、industry、rating、phone、website、billing_address、shipping_address、owner、description | 客户公司实体 |
| `contacts` | account_id、name、email、phone、role、opt_out | 一个客户多个联系人 |
| `products` | code、name、category、manufacturer、hs_code、unit_price、sales_price、stock、status、description | 产品主数据 |
| `quotes` | account_id、contact_id、number、subject、status、valid_until、currency、subtotal、discount、tax、shipping、grand_total、terms | 报价单 |
| `quote_items` | quote_id、product_id、name_snapshot、quantity、unit_price、discount、amount | 报价明细快照 |
| `sales_orders` | account_id、contact_id、quote_id、number、status、po_number、currency、grand_total、delivery_date、terms | 订单 |
| `order_items` | order_id、product_id、name_snapshot、quantity、unit_price、amount | 订单明细快照 |
| `documents` | account_id、order_id、type、number、template_id、version、status、file_path、error_message | PI/CI/PL/报关单 |
| `document_items` | document_id、product_id、description_snapshot、quantity、unit_price、amount | 单据明细快照 |
| `templates` | kind、name、version、source、file_path、schema_json、status、confidence | 固定、自定义、图片识别模板 |
| `private_files` | path、sha256、mime、size、source、main_category、parse_status、index_status、error_message | 本地私有文件 |
| `file_tags` | file_id、tag_id | 文件多标签关系 |
| `tags` | name、scope | 标签字典 |
| `agents` | openclaw_agent_id、display_name、category、description、config_path、status | Agent 注册表 |
| `agent_sessions` | agent_id、started_at、ended_at、message_count、model | 会话记录 |
| `agent_messages` | session_id、role、content、created_at | 对话与用户操作记录 |
| `message_sources` | message_id、source_type、title、uri、file_id、quote_text | 本地/RAG/互联网引用 |
| `jobs` | name、kind、schedule、enabled、last_run、next_run、status、error | 调度任务 |
| `audit_logs` | action、entity_type、entity_id、result、operator、created_at | 增删改、导出、模型、升级操作 |
| `settings` | key、encrypted_value、updated_at | 单用户设置 |
| `suppliers` | company、phone、contact、email、product、specification、quote、notes、source、updated_at | 供应商档案；认证、产能、MOQ 等后续扩展 |

所有金额必须同时保存数值、币种和计算来源；客户订单数和累计金额由订单聚合查询，不在客户表手工维护。

## 7. Go REST API 设计

OpenClaw 编排接口已经由 Go 单体服务实现；业务页面仍主要通过 `data-action` 模拟行为，后续按本章接口逐项替换。

### 7.0 当前已实现接口

| 方法与接口 | 实现逻辑 | 安全边界 |
|---|---|---|
| `GET /api/health` | 返回 Go 服务状态和当前时间，`prototype=false` | 不包含业务数据 |
| `GET /api/v1/openclaw/status` | 限时调用 `gateway status --json --require-rpc`，映射版本、systemd、Gateway、RPC 和审计结果 | 不返回配置路径、令牌或连接凭据 |
| `GET /api/v1/openclaw/models` | 并行调用 `models status/list --json`，映射默认模型、可用模型和凭据数量 | 不透传 OpenClaw 原始 auth 标签或密钥摘要 |
| `PUT /api/v1/openclaw/models/default` | 校验模型 ID 后调用 `models set` | 要求 `X-STA100-Request: 1`，命令超时 |
| `POST /api/v1/openclaw/models/auth` | 校验 provider 和 Key，通过 stdin 调用 `paste-api-key` | Key 不进入参数、响应或日志；请求体限 16 KiB |
| `GET /api/v1/openclaw/agents` | 从 OpenClaw 结构化配置只解析 Agent 安全字段，读取失败时回退 `agents list --json` | 不读取/返回其它配置段 |
| `POST /api/v1/openclaw/agents/sync` | 执行 `sync-agents.sh`，再读取实际 Agent 列表 | 要求写请求头，10 分钟上限，脚本幂等 |
| `POST /api/v1/agents/chat` | 校验 Agent 是否注册；校验消息最多 32 KiB、会话键和三类来源；将用户消息写入临时 0600 文件后调用 `openclaw agent --message-file --session-key --json`，解析真实文本 | 要求写请求头；Agent 白名单；不把消息内容放进 CLI 参数；CLI 90 秒、HTTP 105 秒超时；不记录完整消息 |

OpenClaw CLI 定位顺序为 `OPENCLAW_BIN`、可执行文件相邻 `openclaw/bin/openclaw`、当前目录相邻路径、系统 PATH；Agent 同步脚本也可通过 `OPENCLAW_AGENT_SYNC_SCRIPT` 覆盖。

### 7.1 通用约定

```json
{
  "data": {},
  "meta": {"request_id":"req_xxx", "generated_at":"2026-08-10T10:00:00+08:00"},
  "error": null
}
```

失败统一返回：`code`、`message`、`details`、`request_id`。列表统一支持 `q、page、page_size、sort、status、updated_from、updated_to`；写入接口事务化并记录审计日志。

### 7.2 页面接口清单

| 页面 | 方法与接口 | 主要实现逻辑 | 数据性质 |
|---|---|---|---|
| 概览 | `GET /api/v1/overview` | 聚合今日六项、订阅状态、推荐和三条新闻；数量来自业务表/日志 | 本地 + 私有 + 互联网 |
| 概览 | `PATCH /api/v1/preferences/recommendation` | 保存开关、频率和关注条件 | 本地设置 |
| 概览 | `GET /api/v1/recommendations` | 查询去重后的推荐结果，返回来源和获取时间 | 互联网/RAG/本地 |
| 概览 | `POST /api/v1/overview/oem-matches` | 校验骑行类目和查询条件；并行查询本地工厂库、私有 RAG 和互联网白名单；归一化字段后按选择的类别排序并截取 Top N；返回工厂详情、来源和评分解释 | 原型为示例；正式为本地 + RAG + 互联网 |
| 概览 | `GET /api/v1/overview/oem-matches/{id}` | 返回工厂基础资料、类目、城市、产能、MOQ、认证、来源链接、抓取时间、字段置信度和匹配解释 | 本地/RAG/互联网混合 |
| 概览 | `POST /api/v1/overview/oem-matches/export` | 按本次查询快照导出匹配报告；记录排序类别、Top N、来源和生成时间 | 文件导出；格式待客户确认 |
| 概览 | `POST /api/v1/overview/customers/search` | 按本地/RAG/混合模式检索统一客户字段；默认本地；可过滤至少存在一种联系方式的记录，并保留来源 | 原型为示例；正式为本地/RAG/互联网 |
| 概览 | `GET /api/v1/overview/customers/{id}` | 返回客户统一字段及字段来源、联系方式、引用链接、抓取时间和去重关联 | 本地/RAG/互联网混合 |
| 概览 | `POST /api/v1/overview/customer-discovery` | 校验国家、城市、类型均为单值；查询候选信息后调用 `CustomerMeasurementAgent`，校验结构化响应并返回客户候选及理由 | 原型为示例；正式为本地 + 允许的公开数据 + 模型生成 |
| 智能体 | `GET /api/v1/agents` | 读取 Agent 注册表、状态和快捷问题 | 本地配置 |
| 智能体 | `GET /api/v1/agents/{id}` | 返回初始化配置摘要、技能和来源权限 | 本地配置 |
| 智能体 | `POST /api/v1/agents/{id}/chat` | 检索业务库和私有库后调用 OpenClaw Agent，记录消息和来源 | 模型生成 + 多来源 |
| 智能体 | `POST /api/v1/agents/weekly-report` | 读取所有 Agent 操作日志并调用周报 Agent，生成 Markdown/PDF | 本地日志 + 模型生成 |
| 客户 | `GET /api/v1/accounts` | SQL 分页、关键字、类型、国家、排序；聚合订单数和金额 | 本地业务 |
| 客户 | `POST /api/v1/accounts` | 校验必填、去重、保存客户和默认地址，识别图片需确认 | 本地业务 |
| 客户 | `GET/PATCH/DELETE /api/v1/accounts/{id}` | 详情、更新、软删/归档；有引用时禁止硬删 | 本地业务 |
| 客户 | `POST /api/v1/accounts/ocr` | 解析名片/照片，输出字段、置信度和待确认项 | 图片/OCR |
| 客户 | `GET /api/v1/accounts/{id}/relations` | 一次返回联系人、报价、订单、单据、产品引用 | 本地业务 |
| 客户 | `GET /api/v1/accounts/export` | 按当前筛选导出 Excel，写审计日志 | 本地业务 |
| 报价 | `GET/POST /api/v1/quotes` | 列表或创建草稿；继承客户信息并计算明细金额 | 本地业务 |
| 报价 | `GET/PATCH/DELETE /api/v1/quotes/{id}` | 详情、草稿编辑、归档；锁定状态禁止编辑 | 本地业务 |
| 报价 | `POST /api/v1/quotes/{id}/deliver` | 校验字段、生成 PDF、保存发送历史 | 本地业务 + 文件 |
| 报价 | `POST /api/v1/quotes/{id}/convert-order` | 仅 Accepted 可转，复制快照和明细，保留来源 | 本地业务 |
| 订单 | `GET/POST /api/v1/orders` | 查询或创建订单草稿，支持报价转换和手动创建 | 本地业务 |
| 订单 | `GET/PATCH /api/v1/orders/{id}` | 详情和状态流转，生产后锁定关键字段 | 本地业务 |
| 订单 | `POST /api/v1/orders/{id}/documents` | 按订单快照生成指定单据或全套 | 本地业务 + 文件 |
| 单据 | `GET/POST /api/v1/documents` | 查询或按订单生成 PI/CI/PL/报关单草稿 | 本地业务 + 文件 |
| 单据 | `GET/PATCH /api/v1/documents/{id}` | 预览、补字段、复核、确认、下载 | 本地业务 + 文件 |
| 模板 | `POST /api/v1/templates/upload` | 接收 DOCX/XLSX/PDF/HTML 或 JPG/PNG/WebP，校验类型、20 MB 上限、SHA-256、模板类型和版本，写入隔离区；当前后端未实现 | 本地文件；页面仅完成选择和校验 |
| 模板 | `POST /api/v1/templates/image-recognition` | 对隔离区图片执行 OCR 和版式识别，返回字段坐标、置信度、未识别项和校正任务；当前后端未实现 | 图片 + OCR；引擎和精度待确认 |
| 模板 | `GET/PATCH /api/v1/templates/{id}` | 读取模板版本、预览和字段映射；保存人工校正结果并记录审计；当前后端未实现 | 本地文件 + 映射配置 |
| 模板 | `POST /api/v1/templates/{id}/publish` | 校验必填字段、预览结果和适用单据后发布不可变版本，可设置默认模板；当前后端未实现 | 本地文件 + 审计日志 |
| 产品 | `GET/POST /api/v1/products` | 产品搜索、筛选、创建、批量导入 | 本地业务 |
| 产品 | `GET/PATCH/DELETE /api/v1/products/{id}` | 详情、更新、停用；被引用时禁止删除 | 本地业务 |
| 供应商 | `GET/POST /api/v1/suppliers` | 关键字查询、排序、创建供应商档案；来源使用受控字典并允许后续扩展 | 本地业务 |
| 供应商 | `GET/PATCH/DELETE /api/v1/suppliers/{id}` | 详情、更新、归档；正式删除前检查 OEM 匹配和产品引用 | 本地业务 |
| 数据库 | `POST /api/v1/private-files/upload` | 保存到客户私有区，校验、解析、分类、标签、索引 | 本地文件 |
| 数据库 | `GET /api/v1/private-files` | 分类、标签、状态和关键字查询 | 本地文件 |
| 数据库 | `GET/PATCH/DELETE /api/v1/private-files/{id}` | 在线预览、修改标签、归档、删除 | 本地文件 |
| 数据库 | `POST /api/v1/private-files/{id}/reindex` | 仅重建单文件或变更片段，避免全量扫描 | 本地文件/RAG |
| 新闻 | `GET /api/v1/news` | 读取缓存新闻，按关注条件、时间、相关度筛选 | 互联网缓存 |
| 新闻 | `POST /api/v1/news/refresh` | 调度推荐 Agent，来源白名单抓取、去重、缓存 | 互联网 + 模型 |
| 设置 | `GET/PATCH /api/v1/settings/model` | 读取/保存模型配置，Key 加密 | 本地配置 |
| 登录 | `GET /api/v1/auth/status` | 返回当前设备用户名和会话状态，不返回密码哈希 | 本地账户 |
| 登录 | `POST /api/v1/auth/login` | 校验设备用户名和密码，成功签发 12 小时 HttpOnly 会话 Cookie | 本地账户 |
| 登录 | `POST /api/v1/auth/logout` | 注销当前服务端会话并清除 Cookie | 本地账户 |
| 登录 | `PATCH /api/v1/auth/account` | 登录后验证当前密码，更新用户名和密码哈希 | 本地账户 |
| 登录 | `POST /api/v1/auth/verify-master` | 仅验证设备维护万能密码，不修改账户数据 | 本地维护 |
| 登录 | `POST /api/v1/auth/reset` | 万能密码验证通过后重置用户名和密码，签发新会话 | 本地维护 |
| 设置 | `POST /api/v1/settings/model/test` | 真实测试 OpenClaw/模型连接，返回耗时和异常 | 外部模型 |
| 设置 | `GET/PATCH /api/v1/plugins` | 读取/保存 OpenClaw 插件绑定状态 | 本地配置 + 插件 |
| 设置 | `GET/PATCH /api/v1/jobs` | 内置/自定义任务开关、频率、状态 | 本地调度 |
| 设置 | `POST /api/v1/agent-backups` | 备份 Agent 配置、技能、会话和操作记录并校验 | 本地文件 |
| 设置 | `GET /api/v1/system/health` | 返回数据库、OpenClaw、模型、索引、存储、版本、连接状态 | 本地状态 |
| 设置 | `POST /api/v1/system/upgrade/import` | 接收管理员选择的离线 `.zip` 包；解析清单并校验签名、版本、ARM64 架构、完整性、依赖和磁盘空间；校验通过后创建数据快照并等待安装确认 | 本地升级包 |
| 设置 | `POST /api/v1/system/upgrade/install` | 在停止写入后安装已校验包，执行数据库迁移和健康检查；成功后自动重启 Go 服务，失败恢复旧版本和快照 | 本地升级包 + 本地数据 |
| 设置 | `GET /api/v1/system/upgrade/history` | 查询升级包摘要、操作人、校验结果、快照、安装结果、重启时间和回滚结果 | 本地审计日志 |

## 8. 非功能与异常设计

### 8.1 数据安全

- 单用户登录已实现：首次账户 `admin/admin`；账户文件默认位于 `~/.config/sta100/auth.json`，目录 `0700`、文件 `0600`，密码使用随机 16 字节盐和 150,000 轮迭代 SHA-256，不保存明文。
- 登录会话为服务端内存令牌和 12 小时 HttpOnly、SameSite=Strict Cookie；除健康检查和登录相关接口外，`/api/v1/*` 均需有效会话，服务重启后需要重新登录。
- 登录页显示当前设备用户名并只要求输入密码。设置/数据安全可验证当前密码后修改用户名和密码；侧栏账户按钮退出登录。
- 万能密码作为设备维护恢复入口，仅在登录页“忘记密码”流程输入，不在页面文案和 API 响应中展示；`verify-master` 只验证不修改账户，`reset` 才写入新凭据。生产交付仍应增加审计、尝试次数限制和可关闭配置。
- 本地私有文件和业务 SQLite 不默认上传云端；模型调用按来源权限和最小必要原则拼装上下文。
- API Key、OpenClaw 凭证和插件凭证加密；日志脱敏，禁止打印完整密钥、完整客户联系方式和文件原文。
- 导出、删除、模型配置、插件绑定、升级、备份均写 `audit_logs`。

### 8.2 离线升级与不丢数据

1. 验证升级包签名、版本、ARM 架构、依赖和磁盘空间。
2. 停止写入并生成 SQLite 在线备份、配置备份、Agent 数据备份。
3. 执行数据库迁移，迁移失败自动恢复旧可执行文件和数据库快照。
4. 启动健康检查，确认页面、数据库、OpenClaw、模型、任务和文件可读。
5. 保留旧版本和升级日志，生成升级结果。

首期搁置在线热升级，不提供自动下载安装或后台静默更新。页面只允许管理员从设备本地选择离线包，安装确认后由 Go 服务执行升级并自动重启；重启期间页面显示服务暂不可用，健康检查恢复后才能继续操作。离线包的签名算法、清单格式、兼容版本范围、回滚保留时间和验收步骤仍需客户确认。

### 8.3 文件处理异常

| 异常 | 页面反馈 | 处理 |
|---|---|---|
| 格式不支持 | 文件级错误 | 保留原文件，允许下载，不建立索引 |
| OCR 失败 | 待人工校正 | 允许手动录入字段，不直接生成正式单据 |
| AI 分类失败 | 未分类 | 文件可预览，用户选择分类后继续 |
| 重复文件 | 重复提示 | 依据 SHA-256，不重复消耗索引资源 |
| 索引失败 | 可重试 | 展示错误原因和重试按钮 |
| 模型不可用 | Agent 卡片待配置/对话返回明确错误 | HTTP 503，不返回假回答 |

## 9. 原型验收清单

- [x] 左侧导航顺序为概览、智能体、客户、报价单、订单、单据、产品库、供应商、数据库、行业新闻、设置。
- [x] 概览中日报开关和六个摘要入口在推荐模块上方。
- [x] 推荐支持展开/收起、设置入口和详情抽屉；行业新闻首页固定三条并支持 MORE。
- [x] 概览保留 OEM 工厂智能匹配、客户统一搜索、本地客户发现三项业务工具；当前用 prototype 数据展示完整交互和来源标识。
- [x] OEM 支持骑行类目、查询、排序类别、Top 3/5/10、详情和报告导出入口；正式类目、评分和报告字段仍为 TODO。
- [x] 客户统一搜索支持本地（默认）、RAG、混合模式及必有联系方式过滤。
- [x] 本地客户发现支持国家、城市、类型单值选择及 CustomerMeasurementAgent 链路展示。
- [x] 24 个智能体按四类展示，每个显示 OpenClaw Agent 映射和三个快捷问题。
- [x] 模型未配置状态有全局提示和设置快捷入口；对话区区分三类来源。
- [x] 客户列表支持搜索、筛选、详情、编辑、导出入口及关联业务操作。
- [x] 报价单支持表格/看板、状态、金额、有效期、客户关系、转订单入口。
- [x] 订单支持状态、进度、来源报价、客户关系及生成单据入口。
- [x] 单据包含 PI、CI、PL、报关单，CO 明确移除；支持模板管理入口以及关键字、类型、状态组合筛选。
- [x] 客户、报价、订单、单据和产品支持前端新增、查看、编辑和删除；报价/订单/单据关系字段支持模糊搜索。
- [x] 客户订单数量、累计金额、最近更新及报价/订单金额使用字段旁箭头排序；三个列表全选与行选择联动，排序后保留选择。
- [x] 报价和订单从产品库选择启用产品，支持多行、数量、单价和金额联动；报价转订单、订单转单据沿用明细快照。
- [x] 抽屉内打开业务表单会正确切换到可见弹窗，弹窗内辅助抽屉可覆盖并返回，不再发生同层遮挡。
- [x] 模板中心区分图片/文件入口，完成本地校验、待接口状态和未实现接口说明；真实上传、OCR、映射和发布保持 TODO。
- [x] 产品库支持卡片/表格、搜索、类别筛选、库存排序、详情和 CRUD。
- [x] 供应商页面支持公司、电话、联系人、邮件、产品、规格、报价、备注、来源等字段的前端 CRUD、搜索、排序和导出。
- [x] 数据库支持分类、上传、文件预览、标签和索引状态；无恢复备份入口。
- [x] 设置包含模型、插件、定时任务、Agent 备份、数据安全、系统信息和离线升级。
- [x] 默认 `admin/admin` 登录、退出、当前密码验证修改账户、下次登录显示新用户名和万能密码重置流程均已实现；Go API 未登录返回 401。
- [x] 设置二级导航使用更高字号、48px 高度和明确选中标记；移动端详情值与操作自动换行。
- [x] 桌面端和移动端布局有稳定尺寸；Lucide 和图片资源本地化，模块与 Agent 使用原应用 Emoji。
- [x] OpenClaw 官方稳定版、Node.js ARM64 运行时、状态备份、部署脚本和 systemd 服务归档到 `openclaw/`。
- [x] OpenClaw Gateway RPC、模型状态和 `main + 24` Agent 已通过真实接口核验；接口响应不含凭据材料。
- [x] 模型选择列表直接读取 OpenClaw；已保存 API Key 只显示配置状态，本次输入支持显示/隐藏且不会破坏密钥安全边界。

## 10. 后续 TODO 总表

1. 客户确认 24 个 Agent 的最终中文名称、初始化提示、技能目录、热点问题、来源权限、联网白名单和对话输出规则。
2. 客户确认除当前 DeepSeek 外是否增加其它模型提供商、可选模型范围、Key 轮换和模型不可用时的运维流程。
3. 客户提供正式客户、产品、订单、报价和报关数据字段及必填项。
4. 客户提供 PI/CI/PL/报关单固定模板、图片样例、纸张尺寸、语言、签章和邮件样式。
5. 客户确认单据保存周期、编号规则、状态流转和导出格式。
6. 客户确认互联网来源白名单、账号授权、访问频率、新闻留存周期和原文链接规则。
7. 调研视频上传支持的编码、是否做视频转写、原视频留存位置和单文件大小上限。
8. 客户确认欧洲/国内节点实际地址、离线模式行为和网络恢复后的同步策略。
9. 客户确认设备离线升级的运维责任人和允许升级时段。
10. 客户已要求保留单用户万能密码 `sta100`；正式交付仍需确认密码轮换、错误次数限制、审计日志和是否允许运维关闭该能力。
11. 客户提供 OEM 骑行类目、查询字段、产能/MOQ/认证口径、评分权重、排序类别和报告样式。
12. 客户确认统一搜索联系方式范围、来源优先级、去重规则和互联网/RAG 的展示字段。
13. 客户确认本地客户发现的国家/城市/客户类型字典、公开数据来源、结果保存和导出要求。
14. 客户确认离线升级包清单、签名算法、兼容范围、回滚窗口和自动重启验收标准；在线热升级暂不实现。
15. 真实数据接入后删除原型数据，补充接口联调、权限、性能和 ARM 设备验收。
