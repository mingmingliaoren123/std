# 设置模块功能与接口自检报告

## 1. 检查范围

- 检查日期：2026-08-15
- 项目目录：`/home/User/gsx/gitdir/std`
- 前端：`sta100-web/assets/app.js`
- Go 后端：`sta100-web/main.go`、`sta100-web/auth.go`、`sta100-web/api_extended.go`
- OpenClaw 编排层：`openclaw-orchestrator/orchestrator`
- OpenClaw 版本：`2026.7.1-2`
- 设置页当前模块：
  - 模型设置
  - 通道绑定
  - 定时任务
  - 智能体备份
  - 数据安全
  - 系统信息
  - 版本升级

本报告只记录本轮检视、自测结果和待客户确认事项。本轮未修改业务代码。

## 2. 总体结论

### 2.1 已经具备可运行实现的部分

1. Go Web 服务、登录鉴权、SQLite 业务数据库和 OpenClaw 适配层可以启动。
2. 模型设置已经能够读取固定 OpenClaw 版本目录、保存模型选择、保存 API Key、设置默认模型，并记录测试状态。
3. 通道目录、通道状态、OpenClaw 登录命令和通道添加接口已经保留。
4. 定时任务已经具备 SQLite 的查询、新增、修改、删除能力。
5. 智能体备份可以把本机 `~/.openclaw/agents` 目录打包为 `tar.gz`。
6. 本机账户支持登录、退出、修改用户名密码和维护密码重置。
7. 系统信息可以读取 Go、SQLite、磁盘、私有文件索引和 OpenClaw 状态。
8. 版本升级页已经具备离线包选择、历史查询和后端接口入口。

### 2.2 当前不能视为完整交付的部分

1. 定时任务只有定义 CRUD，没有真正的调度执行器。
2. 离线升级没有实现 manifest、签名、兼容性校验、数据迁移、回滚和自动重启。
3. 智能体备份只有创建，没有列表、恢复、删除、加密和保留周期。
4. 通道目录是固定版本目录；飞书显示为“可用/已安装”不等于已有飞书账号，当前飞书账号数为 0。
5. 通道扫码登录没有独立二维码展示、登录会话轮询或完成回调。
6. 模型测试并非所有 OpenClaw 目录中的提供商都能真实探测。OpenClaw 支持一个提供商，不代表 STA-100 当前已经实现该提供商的连通性探测器。
7. 系统健康接口在当前环境约 12 秒返回，并可能将实际运行中的 OpenClaw 判断为 degraded。
8. 当前页面没有插件设置 Tab；OpenClaw 插件接口和 SQLite 演示插件接口同时存在，容易产生概念混淆。
9. 原始客户数据、私有知识库正文解析、向量索引和联网数据源仍然等待正式数据格式及来源规则。

## 3. 当前运行态自测结果

自测使用本机启动脚本：

```bash
/home/User/gsx/gitdir/std/start-sta100.sh start
```

访问地址：

```text
http://127.0.0.1:18080
```

### 3.1 自测结果表

| 项目 | 请求 | 实测结果 | 耗时/状态 | 结论 |
|---|---|---|---|---|
| 健康检查 | `GET /api/health` | 服务入口可访问 | 正常 | 已实现 |
| 未登录状态 | `GET /api/v1/auth/status` | 返回 `authenticated=false`、用户名 `admin` | HTTP 200，约 46 ms | 已实现 |
| 登录 | `POST /api/v1/auth/login` | `admin/admin` 登录成功 | HTTP 200，约 144 ms | 已实现 |
| 模型设置 | `GET /api/v1/settings/model` | 固定目录 91 个模型，已配置模型 1 个，默认 `minimax/MiniMax-M3` | HTTP 200，约 54 ms | 已实现，目录数量以当前代码为准 |
| 当前模型凭据 | 同上 | DeepSeek、MiniMax、Novita 各有已配置凭据记录 | HTTP 200 | 凭据提供商数量不等于已配置模型数量 |
| OpenClaw 通道目录 | `GET /api/v1/openclaw/channels` | 返回 28 个通道 | HTTP 200，约 41 ms | 已实现，固定目录 |
| 飞书状态目录 | 同上 | `installed=true`、`accountCount=0`、`status=installed` | HTTP 200 | 可用但未绑定账号 |
| 飞书实际状态 | `GET /api/v1/openclaw/channels/feishu/status` | 在 20 秒内未返回 | 超时 | 当前状态查询链路需优化 |
| 定时任务 | `GET /api/v1/jobs` | 返回 5 条任务，其中 4 条内置任务、1 条历史自定义任务 | HTTP 200，约 49 ms | CRUD 数据已存在 |
| 偏好设置 | `GET /api/v1/settings/preferences` | 返回新闻频率、数量、来源和 Agent 设置 | HTTP 200，约 47 ms | 已实现 |
| Agent 列表 | `GET /api/v1/openclaw/agents` | 返回 24 个业务 Agent | HTTP 200，约 50 ms | 已实现 |
| 升级历史 | `GET /api/v1/system/upgrade/history` | 空列表 | HTTP 200，约 38 ms | 查询接口已实现，暂无真实记录 |
| 系统健康 | `GET /api/v1/system/health` | SQLite 和磁盘正常，OpenClaw 返回失败，整体 `degraded` | HTTP 200，约 12.35 秒 | 有真实可复现的耗时和状态判定问题 |
| OpenClaw 独立状态 | `GET /api/v1/openclaw/status` | 返回版本 `2026.7.1-2`、服务 running、RPC 正常 | HTTP 200，约 10 秒 | OpenClaw 实际进程在运行，但状态命令较慢 |
| 模型测试非法参数 | `POST /api/v1/settings/model/test`，模型值 `invalid` | 返回模型 ID 格式错误 | HTTP 200，约 46 ms | 参数校验已实现 |
| 升级导入缺少规范 | `POST /api/v1/system/upgrade/import` | 返回 `TODO_UPGRADE_PACKAGE_SPEC` | HTTP 501，约 41 ms | 明确 TODO，未伪装成功 |
| 新建任务空参数 | `POST /api/v1/jobs` | 返回 `INVALID_JOB` | HTTP 400，约 52 ms | 参数校验已实现 |
| 维护密码错误 | `POST /api/v1/auth/verify-master` | 返回 `INVALID_MASTER_PASSWORD` | HTTP 401，约 48 ms | 错误路径已实现 |
| OpenClaw 插件目录 | `GET /api/v1/openclaw/plugins` | 返回 68 个固定插件，49 启用、19 未启用 | HTTP 200，约 35 ms | 后端存在，当前设置页未使用 |
| SQLite 演示插件 | `GET /api/v1/plugins` | 返回微信、飞书 2 条本地记录 | HTTP 200，约 42 ms | 与 OpenClaw 插件目录不是同一接口 |

### 3.2 自动化检查

以下检查已通过：

```text
openclaw-orchestrator: go test ./...
sta100-web:            go test ./...
sta100-web/assets:     node --check assets/app.js
sta100-web/scripts:     node --check scripts/browser-smoke.mjs
```

当前没有执行真实浏览器点击测试，因为本机 CDP `127.0.0.1:9223` 未启动。接口自测不能代替浏览器布局、弹窗、扫码页面和长时间加载状态的真实验收。

## 4. 设置模块接口梳理

## 4.1 模型设置

### 页面功能

前端位置：

- `sta100-web/assets/app.js:1162-1182`
- 新建/编辑弹窗：`sta100-web/assets/app.js:1408-1449`
- 模型测试和列表测试：`sta100-web/assets/app.js:2637-2667`

当前页面功能：

1. 只展示已配置模型列表。
2. 新建模型时先选择模型系列，再选择一个具体小版本。
3. 输入 API Key，默认隐藏，可临时查看。
4. 选择 MiniMax 国内/国际接入区域，或自动识别。
5. 测试连通性。
6. 保存配置。
7. 对已配置模型执行测试、编辑、设置默认模型、删除。
8. 默认模型只有一个。

### 接口矩阵

| 方法 | 接口 | 当前调用位置 | 请求参数 | 实现逻辑 | 数据落点 | 当前状态 |
|---|---|---|---|---|---|---|
| `GET` | `/api/v1/settings/model` | 设置页进入模型 Tab、刷新模型 | 无 | 读取 OpenClaw 配置文件中的默认模型、凭据状态和模型选择；合并编排层固定模型目录；读取 SQLite 中的测试状态 | OpenClaw 配置、OpenClaw Agent SQLite、STA-100 SQLite `settings.model_test_states` | 已实现 |
| `PATCH` | `/api/v1/settings/model` | 保存、新建、编辑、删除、设置默认 | `model`、`defaultModel`、`provider`、`apiKey`、`endpointMode`、`selectedModels` | API Key 非空时写入 OpenClaw 凭据库；根据 MiniMax 区域写入提供商 `baseUrl`；将默认模型和候选模型写入 `agents.defaults.model/models` | OpenClaw 凭据库、OpenClaw 配置文件、STA-100 审计日志 | 已实现 |
| `POST` | `/api/v1/settings/model/test` | 新建/编辑弹窗测试、列表测试 | 同 PATCH，API Key 可为空 | 先写入 API Key；根据区域写入 MiniMax 接入地址；加载已保存 Key；按提供商发送最小真实请求；记录测试结果 | OpenClaw 凭据库、OpenClaw 配置、STA-100 `model_test_states`、审计日志 | 部分实现 |
| `PUT` | `/api/v1/openclaw/models/default` | 当前前端未调用，历史兼容接口 | `model` | 调用编排层配置默认模型逻辑 | OpenClaw 配置文件 | 后端保留，页面未使用 |
| `POST` | `/api/v1/openclaw/models/auth` | 当前前端未调用，历史兼容接口 | `provider`、`apiKey` | 将 Key 写入 OpenClaw Agent 凭据库 | OpenClaw 凭据库和配置 | 后端保留，页面未使用 |
| `GET` | `/api/v1/openclaw/models` | 当前模型设置页未调用 | 无 | 调用 OpenClaw `models status --json`，并合并固定目录 | OpenClaw CLI、配置文件 | 后端保留，页面未使用 |

### 模型保存与测试的真实链路

```text
页面选择 model/provider
  -> Go 校验模型格式和 provider 是否一致
  -> SaveAPIKey 写入 OpenClaw Agent SQLite 凭据库
  -> 必要时写入 models.providers.<provider>.baseUrl
  -> 测试接口直接调用提供商最小请求
  -> 成功后写入 STA-100 model_test_states
  -> 保存接口把 selectedModels/defaultModel 写入 OpenClaw agents.defaults
  -> Agent 实际聊天时读取 OpenClaw 配置使用
```

### 已核实的模型数据性质

1. 当前设置页模型目录是固定版本目录，不是每次从远程提供商动态拉取。
2. 当前 API 实测返回 91 个目录模型；`sta100-web/README.md` 中“106 个文本模型”的描述已经与实际返回数量不一致，应后续统一。
3. `configuredModels` 只反映当前写入 `agents.defaults.model/models` 的模型，不表示所有已有 API Key 的提供商下所有模型都已配置。
4. `ProviderStatus.configured=true` 只说明提供商有凭据配置，不表示具体模型 ID 一定存在、账户一定有权限或账户有余额。
5. API Key 保存和真实连通性测试是两个操作。当前保存接口允许格式合法但尚未验证有效性的 Key 保存；真实有效性由测试接口判断。
6. 当前测试接口会先把 Key 写入 OpenClaw，再发起真实请求。测试失败后，失败 Key 仍可能已经写入 OpenClaw，这是当前实现行为。
7. 当前只对部分提供商实现了真实探测：
   - OpenAI 兼容：DeepSeek、Mistral、Moonshot、Novita、NVIDIA、Together、火山/字节等
   - Anthropic：Anthropic
   - MiniMax：Anthropic Bearer 兼容入口
   - Cohere：Cohere
   - 其它固定目录提供商可能返回“尚未接入真实探测”
8. 因此“OpenClaw 的模型目录支持”与“STA-100 页面可以测试该模型 API Key”目前不是一一等价关系。

### 模型待确认事项

1. 客户是否要求所有固定目录模型都必须有真实测试适配，还是允许部分模型只保存配置、不提供页面连通性测试？
2. 测试失败后，API Key 是否保留在 OpenClaw 中？当前行为是保留，若不允许，需要改为临时凭据或失败回滚。
3. 一个提供商只有一个 API Key，还是同一个提供商需要支持多个账户、多个 Key 和切换优先级？
4. 默认模型是否只能从“真实测试通过”的模型中选择？当前接口可以设置已配置但未测试通过的模型。
5. MiniMax 国内站、国际站是否需要客户手工选择，还是长期只按 Key 前缀自动判断？
6. OpenClaw 版本升级后，模型目录是否必须由开发重新生成并随版本提交，还是允许启动时读取 OpenClaw 真实目录？
7. 当前模型目录中是否允许配置带自定义 provider 的模型？如果允许，需要补充 provider、base URL、协议类型和测试规则。

## 4.2 通道绑定

### 页面功能

前端位置：

- `sta100-web/assets/app.js:1138-1156`
- 通道弹窗和登录：`sta100-web/assets/app.js:1510-1546`

当前页面功能：

1. 搜索通道。
2. 显示通道名称、ID、来源、安装状态和账号数量。
3. 查看通道状态。
4. 进入通道绑定弹窗。
5. 填写账号标识并调用 OpenClaw 登录流程。

### 接口矩阵

| 方法 | 接口 | 请求参数 | 实现逻辑 | 当前状态 |
|---|---|---|---|---|
| `GET` | `/api/v1/openclaw/channels` | 无 | 返回编排层固定版本通道目录；当前不执行慢的 `channels list` | 已实现，返回 28 条 |
| `GET` | `/api/v1/openclaw/channels/{channel}/status` | 路径通道 ID | 调用 `openclaw channels status --channel {channel} --json` | 已实现，但无账号时可能等待至 20 秒超时 |
| `POST` | `/api/v1/openclaw/channels/{channel}/login` | `account` | 调用 `openclaw channels login --channel ... --account ...` | 已实现命令调用，扫码体验未闭环 |
| `POST` | `/api/v1/openclaw/channels/{channel}` | 通道账号、Token、Secret、URL 等 | 调用 `openclaw channels add`；敏感字段通过临时 0600 文件传递 | 后端已实现，当前页面未使用 |

### 已核实的通道数据性质

1. 当前固定目录共 28 个通道。
2. 飞书：`installed=true`、`origin=available`、`accountCount=0`。
3. 微信：`openclaw-weixin`，当前标记为 `installable`，不是已安装可直接扫码状态。
4. 当前通道目录的 `accountCount` 来自固定目录，默认是 0，不是每次从 OpenClaw 真实账号列表计算。
5. 当前前端将 `installed=true` 纳入“已安装/已配置”统计，导致飞书在账号数为 0 时也被计入；该统计名称和业务语义不准确。
6. 当前通道状态查询确实调用 OpenClaw CLI，但自测飞书状态在 20 秒内没有返回，说明没有账号时的 CLI 行为不适合直接作为同步页面请求。
7. 当前扫码登录接口只把 CLI 命令提交给 OpenClaw，页面没有二维码图片、登录事件流、轮询进度、成功回调和失败重试状态。

### 通道待确认事项

1. 第一阶段需要正式支持哪些通道：飞书、微信、企业微信、Telegram，还是仅飞书和微信？
2. “扫码绑定”是否必须在 STA-100 页面直接显示二维码？如果是，需要确认二维码来源、刷新周期、有效期和登录成功判定。
3. 一个通道是否允许绑定多个账号？账号名称、默认账号和切换规则是什么？
4. 通道是只允许已安装通道绑定，还是页面要支持安装通道包？如果支持，离线安装包来源和安全校验需要定义。
5. 通道消息是只接收通知，还是需要支持双向发送、文件、图片、群聊和消息历史同步？
6. 通道账号凭据是否全部由 OpenClaw 管理，STA-100 是否禁止保存任何通道 Token？
7. 通道状态的刷新周期、离线判定和错误提示标准是什么？

## 4.3 定时任务

### 页面功能

前端位置：

- 列表：`sta100-web/assets/app.js:1158-1160`
- 新建/编辑弹窗：`sta100-web/assets/app.js:2707-2725`

可编辑字段：

- 任务名称
- 任务类型
- 执行频率/Cron 文本
- 任务说明
- 执行 Agent
- Prompt
- 是否启用

### 接口矩阵

| 方法 | 接口 | 请求参数 | 实现逻辑 | 当前状态 |
|---|---|---|---|---|
| `GET` | `/api/v1/jobs` | 无 | 从 SQLite `jobs` 记录读取任务 | 已实现 |
| `POST` | `/api/v1/jobs` | `name`、`kind`、`schedule`、`description`、`agentId`、`prompt`、`enabled` | 校验名称和类型，生成任务 ID，写入 SQLite | 只实现定义保存 |
| `PATCH` | `/api/v1/jobs` | 必须带 `id` | 修改任务定义并写审计日志 | 已实现 |
| `PATCH` | `/api/v1/jobs/{id}` | 任务字段 | 修改指定任务，保留部分原值 | 已实现 |
| `DELETE` | `/api/v1/jobs/{id}` | 路径 ID | 自定义任务软删除；内置任务拒绝删除 | 已实现 |

### 当前内置任务

| ID | 任务 | 当前 Prompt/作用 | 当前状态 |
|---|---|---|---|
| `JOB-RECOMMEND` | 每日推荐更新 | 根据关注国家、主题和本地业务数据生成推荐摘要 | `WaitingSource` 或类似待数据状态 |
| `JOB-WEEKLY` | 智能体周报 | 汇总近 7 天 Agent 使用、业务操作和待跟进事项 | 当前周报接口实际是本地汇总草稿 |
| `JOB-NEWS` | 行业新闻更新 | 围绕骑行行业、欧洲渠道、法规和产品趋势整理新闻 | 等待正式新闻来源 |
| `JOB-INDEX` | 数据索引维护 | 检查私有文件解析、分类、去重和索引 | 等待原始数据格式 |

### 当前问题

1. 代码中没有 cron 调度器、后台 worker、执行记录写入或失败重试逻辑。
2. `schedule` 当前是普通字符串，后端没有校验 Cron 语法、时区、最小间隔和重复任务。
3. `enabled=true` 只代表任务定义启用，不代表任务会执行。
4. `LastRun`、`NextRun`、`LastResult` 字段已存在，但没有执行器维护。
5. 页面显示的 `Active/Review/Draft` 是任务定义状态，不是实际执行成功状态。

### 定时任务待确认事项

1. 是否正式接入后台调度执行器？
2. 设备使用哪个时区，断电、重启、休眠后如何补执行？
3. Cron 是否对客户开放，还是只允许预置频率？
4. 每个内置任务的实际执行内容、数据范围、输出位置和通知渠道是什么？
5. 任务失败是否重试，重试次数和间隔是多少？
6. 任务执行结果保存多久，是否需要下载和审计？
7. 任务是否需要通过飞书、微信或邮件通知？
8. Agent 执行任务时是否使用 Agent 单独配置模型，否则使用默认模型？

## 4.4 智能体备份

### 页面和接口

前端入口：设置 → 智能体备份 → 立即备份。

接口：

```text
POST /api/v1/agent-backups
```

当前实现逻辑：

1. 查找 `~/.openclaw/agents`。
2. 在 `~/.local/share/sta100/agent-backups` 创建目录。
3. 生成 `agents-YYYYMMDD-HHMMSS.tar.gz`。
4. 文件权限为 0600。
5. 写入 STA-100 审计日志。
6. 返回路径、大小、创建时间和 `restoreSupported=false`。

### 当前数据范围

实际只打包 `~/.openclaw/agents`。当前没有包含：

- STA-100 SQLite 业务数据库
- OpenClaw 主配置
- OpenClaw 凭据库/API Key
- 私有文件正文
- 私有文件索引
- 升级包
- 备份历史索引

因此页面“智能体初始化配置、技能、会话和用户操作记录”的表述大于当前接口实际范围，尤其用户操作审计日志不在 Agent 目录内。

### 待确认事项

1. 备份范围是否只要 Agent 数据，还是必须包含业务库、设置、私有文件索引和 OpenClaw 配置？
2. API Key 和通道凭据是否备份？如果备份，是否加密以及谁可以恢复？
3. 是否需要恢复功能？恢复前是否需要停服务和自动备份当前版本？
4. 备份保存位置是本机磁盘、TF 卡还是指定外置目录？
5. 备份保留数量、保留天数和自动清理规则是什么？
6. 备份包是否需要版本号、设备号、校验和和签名？

## 4.5 数据安全和本机账户

### 页面功能

前端位置：`sta100-web/assets/app.js:1130-1132`。

当前页面包含：

- 本机登录账户
- 本地存储区说明
- 敏感信息保护说明
- 联网调用边界说明
- 操作记录说明

### 接口矩阵

| 方法 | 接口 | 作用 | 数据落点/状态 |
|---|---|---|---|
| `GET` | `/api/v1/auth/status` | 查询当前登录状态和当前用户名 | 内存会话、`auth.json` |
| `POST` | `/api/v1/auth/login` | 用户名密码登录，签发 HttpOnly Cookie | 内存会话 |
| `POST` | `/api/v1/auth/logout` | 注销当前会话 | 内存会话和 Cookie |
| `POST` | `/api/v1/auth/verify-master` | 校验维护万能密码 | 当前为硬编码 `sta100` |
| `POST` | `/api/v1/auth/reset` | 使用维护密码重置用户名和密码 | `~/.config/sta100/auth.json` |
| `PATCH` | `/api/v1/auth/account` | 登录后验证当前密码并修改用户名/密码 | `~/.config/sta100/auth.json` |

当前安全实现：

1. 默认账号密码均为 `admin`。
2. 密码不保存明文，使用随机盐和 150,000 轮 SHA-256 派生。
3. 会话 Cookie 为 HttpOnly、SameSite=Strict，默认 12 小时。
4. 业务写操作要求 `X-STA100-Request: 1`。
5. SQLite、账户文件和密钥临时文件使用受限权限。
6. 关键模型、升级、业务写操作会写审计日志。

### 当前风险和待确认事项

1. 维护万能密码固定为 `sta100`，没有设备级唯一密钥和轮换机制。
2. 只支持单一管理员账户，没有角色权限和多用户。
3. 没有登录失败次数限制、账号锁定、验证码或细粒度登录审计页面。
4. 当前密码最短 4 个字符，是否满足正式安全要求需要确认。
5. 页面说明“操作记录”已记录，但当前没有设置页审计日志查询接口。
6. 客户是否需要区分管理员、业务用户、只读用户和运维用户？
7. 忘记密码是否允许所有现场人员使用万能密码，还是只能通过维护介质/设备管理员恢复？
8. 是否需要数据导出、备份和升级操作的二次确认或管理员密码确认？
9. 是否需要脱敏规则、日志保留周期和敏感字段访问审计？

## 4.6 系统信息

### 页面功能

前端位置：`sta100-web/assets/app.js:1548-1564`。

展示：

- Go 运行环境
- 服务健康
- OpenClaw 版本与 RPC
- Agent 编排数量
- SQLite 状态和大小
- 存储空间
- 私有文件索引状态

### 接口矩阵

| 方法 | 接口 | 作用 | 当前状态 |
|---|---|---|---|
| `GET` | `/api/v1/openclaw/status` | 调用 `openclaw gateway status --json --require-rpc` | 可返回 running/RPC 状态，但当前约 10 秒 |
| `GET` | `/api/v1/openclaw/agents` | 查询 OpenClaw 当前业务 Agent | 返回 24 个业务 Agent |
| `POST` | `/api/v1/openclaw/agents/sync` | 按 STA-100 manifest 同步 Agent | 后端已实现，页面有同步按钮 |
| `GET` | `/api/v1/system/health` | 汇总 OpenClaw、SQLite、磁盘、私有文件和索引状态 | 已实现，但耗时和状态判定有问题 |

### 已复现的问题

1. `/api/v1/openclaw/status` 可返回：
   - OpenClaw `2026.7.1-2`
   - systemd user service running
   - Gateway RPC `ok=true`
2. `/api/v1/system/health` 内部给 OpenClaw 状态调用设置 10 秒超时。
3. 当前 OpenClaw status 命令本身可能接近或超过 10 秒。
4. 因此系统健康接口返回：
   - SQLite 正常
   - 磁盘正常
   - OpenClaw `ok=false`
   - 总体 `degraded`
5. 这不一定表示 OpenClaw 进程停止，更可能是健康检查超时阈值过短或调用方式过重。

### 其它待确认事项

1. 系统健康接口允许的最大响应时间是多少？
2. OpenClaw 状态检查是实时同步检查，还是读取后台缓存？
3. OpenClaw 暂时不可访问时，页面应显示“服务异常”“检查超时”还是“暂不可确认”？
4. “知识索引 ready”的验收条件是什么？当前原始数据格式未提供，状态为 `waiting_raw_data_format`。
5. 待办和会议数据当前固定为 0，客户是否提供日历、任务或业务数据来源？
6. 当前版本、构建日期和硬件架构是否应该由后端动态读取？目前升级页版本显示是前端固定文本。

## 4.7 版本升级

### 页面功能

前端入口：设置 → 版本升级。

当前页面显示：

- 当前版本
- 升级保护说明
- 选择本机 `.zip` 离线升级包
- 查看升级记录
- 页面文案声明安装后自动重启

### 接口矩阵

| 方法 | 接口 | 当前实现 |
|---|---|---|
| `GET` | `/api/v1/system/upgrade/history` | 固定返回空记录 |
| `POST` | `/api/v1/system/upgrade/import` | 固定返回 `501 TODO_UPGRADE_PACKAGE_SPEC` |

前端本地只检查：

- 文件扩展名是否为 `.zip`
- 文件是否已选择

后端尚未执行：

- manifest 解析
- 产品/设备/版本匹配
- ARM64 架构校验
- 数字签名校验
- 文件完整性校验
- 磁盘空间检查
- 数据库迁移
- 配置迁移
- OpenClaw 兼容检查
- 安装过程
- 自动重启
- 失败回滚
- 升级记录写入

### 待确认事项

1. 升级包目录结构和 manifest 字段。
2. 签名算法、证书、公钥存放位置和密钥轮换。
3. 支持的设备型号、架构和最低/最高版本。
4. 是否允许跨多个大版本升级。
5. SQLite 数据迁移和失败恢复策略。
6. OpenClaw 配置、凭据、Agent 和私有文件的迁移策略。
7. 升级失败是否自动回滚，回滚是否需要人工确认。
8. 升级包暂存目录、最大大小和清理规则。
9. 自动重启时间、重启后页面提示和升级锁定状态。
10. 升级记录是否需要包含操作人、包版本、校验和、结果和失败原因。
11. 设备离线时是否允许通过 TF 卡升级。

## 5. 相关但不在设置 Tab 中的功能

## 5.1 新闻/推荐偏好

相关接口：

```text
GET/PATCH /api/v1/settings/preferences
GET/PATCH /api/v1/preferences/recommendation
POST      /api/v1/news/refresh
PATCH     /api/v1/overview/subscription
```

当前已实现：

- 展示数量 1-100 校验。
- 获取频率限定为 1、2、3、6、8、12、24 小时。
- 国家、主题、来源文本保存。
- 推荐订阅开关保存。

当前未完成：

- `POST /api/v1/news/refresh` 仍依赖正式新闻来源规则。
- 没有联网来源白名单实际抓取器。
- 没有来源鉴权、失败重试、去重、更新时间和留存机制。
- 当前“行业新闻更新”定时任务不会真正执行。

客户需要确认：

1. 新闻来源是固定网站、RSS、客户提供 API，还是 OpenClaw 联网能力？
2. 是否允许 Agent 自主联网，还是必须限制域名白名单？
3. 来源失败时是否保留旧数据？
4. 新闻正文、摘要、图片和原文链接保存多久？
5. 新闻刷新是否需要通过通道推送？

## 5.2 插件与通道的接口混用问题

当前同时存在两套“插件”概念：

### OpenClaw 固定插件目录

```text
GET  /api/v1/openclaw/plugins
POST /api/v1/openclaw/plugins/{pluginId}
```

实测返回 68 个插件，来自编排层固定目录 `openclaw-2026.7.1-2`。启用/禁用会调用真实 OpenClaw CLI。

### STA-100 SQLite 演示记录

```text
GET   /api/v1/plugins
PATCH /api/v1/plugins
PATCH /api/v1/plugins/{id}
```

实测返回“微信”“飞书”两条本地记录。修改只更新 SQLite 状态，并返回“插件绑定凭据、推送内容和同步范围待客户确认”，不会真实完成 OpenClaw 通道绑定。

### 建议确认

1. 设置页是否删除 SQLite 演示插件接口，只保留 OpenClaw 真实通道/插件接口？
2. 客户是否需要单独的“插件管理”页面？
3. 如果只需要模型和通道，插件目录是否只作为 OpenClaw 内部依赖，不对客户展示？
4. 飞书、微信应按“通道”管理，不应在插件页面伪装成已绑定。

## 6. 前端、后端、OpenClaw 调用链对照

### 模型

```text
设置页模型弹窗
  -> PATCH/POST /api/v1/settings/model*
  -> businessAPI.settingsModelRouter
  -> orchestrator.SaveAPIKey / SetProviderBaseURL / SetConfiguredModelSelection
  -> OpenClaw auth_profile_store、openclaw.json
  -> Agent 真实调用读取 OpenClaw 配置
```

### 通道

```text
设置页通道列表
  -> GET /api/v1/openclaw/channels
  -> 固定 OpenClaw 版本目录

点击状态
  -> GET /api/v1/openclaw/channels/{id}/status
  -> openclaw channels status

点击扫码绑定
  -> POST /api/v1/openclaw/channels/{id}/login
  -> openclaw channels login
  -> 当前页面没有二维码和异步状态闭环
```

### 定时任务

```text
设置页任务表单
  -> POST/PATCH/DELETE /api/v1/jobs
  -> SQLite jobs 记录
  -> 当前没有后台调度器
```

### Agent 备份

```text
设置页立即备份
  -> POST /api/v1/agent-backups
  -> 读取 ~/.openclaw/agents
  -> 本地 tar.gz
  -> 写审计日志
```

### 系统信息

```text
系统信息页
  -> /api/v1/openclaw/status
  -> OpenClaw gateway status CLI

  -> /api/v1/openclaw/agents
  -> OpenClaw Agent 清单/配置

  -> /api/v1/system/health
  -> SQLite + 磁盘 + 私有文件记录 + OpenClaw status
```

## 7. 建议的客户确认优先级

### 高优先级

1. 模型测试范围：是否要求所有 OpenClaw 模型都支持真实 API Key 测试。
2. 模型测试失败后的 Key 是否回滚。
3. 默认模型只能选择真实测试通过的模型，还是允许仅保存。
4. 第一阶段实际支持的通道以及扫码绑定体验。
5. 定时任务是否必须真实执行，以及 4 个内置任务的正式业务定义。
6. 离线升级包格式、签名、迁移、回滚和自动重启方案。
7. 备份范围和恢复要求。
8. 单管理员还是多用户/角色权限。

### 中优先级

1. 系统健康检查的响应时间和降级判定。
2. 通道账号数量、状态刷新、多个账号管理。
3. 新闻来源、联网白名单、失败重试和保留周期。
4. 审计日志查询、导出和保存周期。
5. OpenClaw 插件是否需要作为独立页面展示。

### 低优先级

1. 备份压缩包命名、展示、下载和清理细节。
2. 模型目录分组名称和模型小版本显示文案。
3. 系统信息页面的展示字段排序。

## 8. 建议验收口径

设置模块不能只以“接口 HTTP 200”作为验收标准，建议按以下口径验收：

1. 模型：
   - 保存后能在 OpenClaw 配置和凭据库中看到对应配置。
   - 有效 Key 测试能真实完成最小模型调用。
   - 无效 Key 测试能明确返回鉴权失败。
   - 无权模型能明确区分为模型权限错误。
   - 测试状态与当前 Key、当前模型、当前区域一致。
2. 通道：
   - 能看到真实安装状态和真实账号数。
   - 扫码页面能看到二维码或明确登录指引。
   - 登录成功后状态能自动刷新为已绑定。
   - 失败、超时、取消有明确状态。
3. 定时任务：
   - 到达执行时间后能真实执行。
   - 任务状态、最近执行时间、下次执行时间、结果和错误可查询。
   - 重启后任务不会丢失，也不会重复执行。
4. 备份：
   - 能列出历史备份。
   - 能验证完整性。
   - 按客户确认范围恢复并验证数据不丢失。
5. 安全：
   - 密钥不回传、不进日志、不出现在前端源码。
   - 登录失败、密码修改、万能密码使用都有审计记录。
   - 角色权限与敏感数据访问符合客户确认规则。
6. 升级：
   - 非法包、错误版本、错误架构、签名错误和空间不足均阻止安装。
   - 升级前后数据库、配置、Agent、模型和私有数据可验证。
   - 失败能回滚，成功后能自动重启并留下记录。

## 9. 本轮最终判断

当前设置模块的“页面入口、Go 路由、基础数据落点”大部分已经建立，但“真实业务闭环”仍集中缺少在通道扫码、任务执行、备份恢复、离线升级和全模型探测几个方向。模型保存和测试链路已经能真实影响 OpenClaw，但不能把“保存成功”直接解释为“模型有效”，也不能把“OpenClaw 有模型目录”直接解释为“STA-100 已支持该模型的真实测试”。

建议先由客户确认本报告第 4 节各模块的待确认事项，尤其是高优先级项目；确认后再决定是修复现有链路、补齐真实执行能力，还是保留当前明确 TODO 的范围。
